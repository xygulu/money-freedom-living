# 06 · 信息架构与认知层验收（补 docs/05 的两处缺口）

> 状态：2026-09-14 · **由服务器侧编码 agent（claudecode）起草**。
> Hermes（方案讨论/验收侧）已逐条验收，**批注在知识库 `让读书真正改变人生/20-7_信息架构与认知层验收.md`**（本文不再重复）。
> **本文不是新方案，只补 `docs/05` 未覆盖的两块**；与 `docs/01–05` 冲突时**以既有文档为准**，本文只在下述两处范围内作为依据：
> ① **信息架构**（一级入口、页面去向、日频"一段"承载在哪一屏）
> ② **认知层验收指标 L4 / L5 / M 的采集落点**
> 依据来源：知识库 `让读书真正改变人生/10-2_认知内化机制_方法与验收`、`20-1_总设计_机制验证方案`、`20-5_产品架构_成长归用户与多书融合`。
>
> ⚠️ **勘误（2026-09-14，Hermes）**：初稿署"由 Hermes 补写"并引用"知识库 `20-6_信息架构_v4`" —— **两处均不实**。该文件在知识库**不存在**（`20-6` 是《v1 数据模型改动清单》，且全库检索"信息架构"0 命中）；本文亦非 Hermes 所写。
> **规矩：引用知识库文件前必须核对文件名，不得凭印象写引用。**

---

## 1. 为什么需要这一份（证据）

对 `docs/*.md` 做关键词检索，下列词**全部 0 命中**：`一条路`、`三个入口`、`一格`、`L4`、`L5`、`停顿`、`机关`、`命题坐标`。

即：`docs/05` 说清了**日频形态是"一段"**（开 / 做 / 合），但没有说**这一段承载在哪一屏、导航怎么改、`/journal`、`/timeline`、`/letters`、`/portrait` 这些页面去哪**；也没有说**今天定案的认知层验收指标（L4 迁移 / L5 停顿 / M 恢复时间）从哪儿采**。这两处不补，编码只能按现状保留 4 Tab、且 M11 做完也拿不到认知层数据。

---

## 2. 信息架构：一级入口 4 → 3

```
┌────────────────────────────────────────────┐
│         路              聊              我   │
│  （今天这一段）  （陪伴对话·产品主体）  （档案） │
└────────────────────────────────────────────┘
```

- **路**＝被动模式：打开就被接住、就被给一段（承载 `docs/05 §8` 的"开 / 做 / 合"）
- **聊**＝主动模式：我想说点什么（现状 `/chat`，保留）
- **我**＝档案模式：只在用户主动想回看时出现

**导航纪律**：新功能**不得直接进一级导航**——只能从"路"上长出来（一段 / 一个机关）、从"聊"里长出来（对话动作），或进"我"。一级入口永远 3 个。

### 2.1 页面去向（现状 → 目标）

| 现状页面 | 去向 | 说明 |
|---|---|---|
| `/`（落地页） | 未登录＝落地页；**已登录直接是"路"** | 已体检用户不再需要中间页（现状已部分如此） |
| `/onboarding` | **保留但重排**：按 `docs/05 §7`「先被说中 → 再注册 / 同意 / 定价」 | 敏感信息同意弹窗挪到"被说中"之后 |
| `/journey` | **＝首页「路」**（`docs/05 §8` 的一段） | 三张卡片退场 |
| `/chat` | **Tab 2「聊」** | 不变 |
| `/journal` | **降级进「我」**＝"我写过的话" | 写字本就是"叙"的一部分，不占一级入口 |
| `/timeline` | **并入「你走过的路」**（`docs/05 §3.5` 里那个唯一露出书的页面） | 时间线＝路的纵轴 |
| `/letters` | **机关节点的动作**（D14 / D30 / D66 / D90） | 信是仪式，不做常驻功能页 |
| `/portrait` | **「我」的第一屏**（我的镜子） | 画像仍是灵魂物件，不占主导航 |
| `/portrait/compare`、`/journey/changes` | **机关节点**（回望 / 90 天带走） | 只在节点出现 |
| `/vip`、`/me`、`/recover`、`/login` | **「我」** | 账号、支付、恢复、设置 |
| `/chat/history` | 「聊」内只读索引 | 保留回看能力 |

### 2.2 首页「路」的结构（自上而下）

```
今日日期（不出现书名 —— 依 docs/05 §3.5：书只在「你走过的路」露出）
┌──────────────────────────────┐
│ 你上次留在这里：                │   ← 昨日回响（没有就留白，不占位）
│ "发薪日我居然没先还花呗"         │
└──────────────────────────────┘
┌──────────────────────────────┐
│ 开：今日一签                    │
│ 做：一个 2 分钟动作             │   ← docs/05 §8 的「开 / 做 / 合」
│ 合：一句话被接住 +「今天到这里」 │
└──────────────────────────────┘
· · · ● ● ● ● ● ● ● ○ ○ ○ ○ ○      ← 走过的格子用用户自己的原话做纹理
```

**硬规矩**：只有**一个主动作**（"说说今天"）／无红点无未读／无任务列表／**书名不出现**。

### 2.3 「合」的那一步＝三件事压成一屏（不做多步流程）

```
① 我做了   [✓ 就是这个]  [换一个]  [今天跳过]    ← 一次点击即确认
② 我想到   [ 一句话 / 一张图 / 语音 ]           ← 「证」
③ 脑子里第一句话 [ 一句话，可跳过 ]              ← 「叙」＝认知层入口（见 §4）
          [ 交给他 ] → 即时见证回应（现状已有）+ 这一段收束
```
②③ 可跳过；③ **跳过不惩罚、不暗示"建议填写"**（它是照见工具，不是作业）。

---

## 3. 「我」的结构（档案模式）

```
我
├─ 我的镜子     画像（当前版）· 命题坐标（六命题上"我到这儿了"）
├─ 我说过的话   原话收藏（《我脑子里的那几句话》滚动版）
├─ 我走过的路   跨书站点 · 机关节点 · 回望图 ← 书唯一露出处（docs/05 §3.5）
├─ 我的记录     日记 / 信 / 对话索引（现状内容聚合，不再抢主界面）
└─ 设置与账号   VIP · 导出 · 删除 · 恢复码 · 语言 · 邮件与通知偏好
```

**命题坐标 = 用户级主进度条**（`docs/05 §3.1/§3.2`：书是"区域"不是进度条；灯从书的阶段里拆出来挂命题）。
→ 界面呈现：六条命题各有"我到这儿了"的刻度；**没有"书的完成度"这个概念**。

---

## 4. 认知层验收指标 L4 / L5 / M（**服务器文档目前缺这一块**）

依据：知识库 `10-2_认知内化机制_方法与验收`（今天的定案）。这三个是判断"机制成不成立"的**主指标**（行为层必须与认知层成对看，只测行为会得出假结论）。

| 指标 | 定义 | 采集落点 | 用户成本 |
|---|---|---|---|
| **L4 迁移** | 没人提示，主动用新框架解释了一件**新事** | ① AI 在对话里识别到"意外用上了新说法"时，一键记下 ② 「合」那一步可手动标记 | 1 次点击 |
| **L5 停顿** | 原来自动化的反应，现在**能停一下**了 | 「合」的第 ③ 问（每天出现，可跳过） | 1 句话 |
| **M 恢复时间** | 掉回旧模式后，**恢复得更快** | **不新增交互**：由"掉回旧模式的记录"与"下次出现的时间差"自动计算 | 0 |

**原则**：认知层采集**不新增第三个日常任务**。能自动算的自动算（M）；能一句话问到的绝不设计成表单（L5）；能一键记的绝不要求用户写（L4）。

**与 `docs/05 §4.5`「6 个阶段验证节点：零 DDL」的关系**：节点（D7/14/21/30/66/90）是**出结论的时刻**；L4/L5/M 是**结论的依据**（也是 20-1 判定表里"机制成立 / 空转 / 无效"三档的判据）。二者必须成对存在，缺一个就只能退回去看行为完成率。

**反指标（必须一起记）**：**形式化打卡率**（只写"已完成"、无细节的占比）与**敷衍率**——高于 30% 说明机制在空转。

---

## 5. 机关节点（D7/14/21/30/66/90）的形态

| 规则 | 内容 |
|---|---|
| **不是报告** | 节点不做"你进步了 30%"这类结论；**给物件，不给结论**（`docs/05 §8.1`：解读权归用户） |
| **物件是什么** | 把他自己的原话按时间排出来 + 回望图（`/portrait/compare`、`/journey/changes` 归到此处） |
| **越往后越轻** | D3–D14 是**节奏节点**（密度高、决定留存）；D21 之后是**观察节点**（每节点只问 1–2 句，不加动作量） |
| **允许提前收敛** | 任一阶段结论明确（成立 / 不成立）即停，不必跑满 90 天 |
| **出口** | 每个节点都要有"今天不聊"的出口 |

---

## 6. 四条自检（设计评审 / 验收时逐条跑）

- [ ] **盖住书名**：首页截图上的书名全部盖住，用户仍清楚"今天做什么、我走到哪了"
- [ ] **一个主动作**：首页只有一个主按钮
- [ ] **3 分钟**：从打开到"合"完成 ≤3 分钟（含第 ③ 问）
- [ ] **断 30 天回来**：界面上不出现任何"你错过了 / 已断签 / 补上"字样

---

## 7. 与 `docs/02 §12` 目标口径的关系（内部指标 vs 体验目标）

`docs/05 §8.2` 的"目标口径修正"（不把"每天主动使用"当目标）**与验证需求不冲突，但要补一句**：

- **体验目标**（对外、进界面与文案）＝ 每次回来有确定的质量 + 想面对钱的那一刻第一个想到它
- **内部验证指标**（不进界面、不进文案、不进触达）＝ **每日日课完成率**（"合"那一步完成 = 1 天），以及 L4 / L5 / M 随时间的序列

理由：认知层证据是**时间序列**（停顿要逐日看、恢复时间要趋势、迁移要间隔出现），一周来一次采不到。**内部测频率 ≠ 对外追频率**：前者是机制验证的必要条件，后者正是 §8.2 要避免的东西。

---

## 8. 本文待确认

- `docs/05 §8.2` 标注的 `[待你确认]`：目标口径按第 7 节"内外分开"处理（待用户拍板后，本节并入 `docs/02 §12` 口径）

---

## 9. 验收发现（2026-09-14 由 Hermes 登记，待修）

> 来源：用户在 M11 改完后观察前端仍显示"看见-松动-练习-活法"，追问其通用性。结论：**骨架通用（认知/心态改变类的功能位），但对外命名必须随书**；下面两处把**书的语汇写在了产品层**，第二本书进来会串味。

| # | 位置 | 现状 | 处置建议 | 级别 |
|---|---|---|---|---|
| 1 | `src/i18n/messages/*/zh-CN.json` 的 `stageLamp*Hint`（"认领的那一刻，**松动**就开始了"、"话语里看得见**松动**"…） | 灯的提示文案含《不愁钱》阶段语汇 | 把带阶段语汇的提示**挪进书的内容层**（与 `title`/`goal` 同处，随书替换）；i18n 只留中性句 | 建议修（多书前必须） |
| 2 | `arcOpen: "开 · **看见**"` | 产品级日频文案嵌了书的阶段名 | 改中性（"开 · 今天的一句话" 或 "开 / 做 / 合"） | 建议修（多书前必须） |

**为什么不是"必须马上改"**：单书阶段不影响使用，两处都只影响**第二本书进来时的语汇正确性**；但必须在**接第二本之前**修掉，否则第二本书的用户会看到第一本书的语汇。

**判定原则（写进验收口径）**：**功能位固定**（觉察 → 解构/替代 → 练习 → 内化）；**对外名称由书/命题提供**（`content/books/<book>/<locale>/journey/` 已实现，属正确做法）。产品级文案（i18n）里**不得出现任何单本书的阶段语汇**。

### 9.1 追加（同步 `c7a3375` 后新发现）

| # | 位置 | 现状 | 处置建议 | 级别 |
|---|---|---|---|---|
| 3 | `docs/05 §9.3` 正文 | 枚举名仍写 `'email_touch'` / `'sensitive_profile'`，与实现（`'touch'` / `'sensitive'`，§4.8 已声明以代码为准）**不一致** | 改 §9.3 正文，与代码对齐 | 建议修（防后来人按错名字写码） |
| 4 | `src/lib/touch.ts` / `deploy/mfl-touch.timer` | **邮件触达代码已实现，但 Resend 未配置** → `touchConfigured()` 为假时 dispatch 降级试算，**至今没有一封信真发出过**；发信域名 SPF/DKIM/DMARC 未配 | **上线/验收必须实测一封真实到达的信**（真邮箱 + 真退订链接）；验收不接受"代码已实现" | **必须验**（否则"温和触达"这条产品承诺等于不存在） |

**验收口径补充**：凡涉及"外部通道"的能力（邮件、通知），验收一律以**真实到达**为准（收件箱截图 + 退订链接可用），不以代码存在为准。

---

## 10. M11 验收结论（2026-09-14 · Hermes 独立核查）

> 方法：读代码 / 只读查库 / 真浏览器走界面 / 配置实况 —— **不以作者自测为准**（`tsc` 0 错、`vitest` 261/261 仅作参考）。
> 完整报告：知识库 `让读书真正改变人生/20-8_M11系统验收报告与优化建议.md`（服务器侧对应件 `docs/07`）。
>
> ⚠️ **勘误（2026-09-14 16:1x，Hermes）**：本行原写 `80_验收/80-1_M11验收报告与产品优化建议_2026-09-14.md` → 已改为**当前**知识库里的真实文件 `20-8_M11系统验收报告与优化建议.md`。
> **说明**：`80_验收/80-1_…` **不是编的** —— 它是**服务器 `docs/kb/` 知识库镜像里的旧文件名**（知识库当天重组改过名，镜像未跟上）。**镜像滞后是验收侧的责任**（镜像 README 声称的同步脚本与 cron 从未落地）。全过程见 `docs/09 §8`。
> 另：`vitest` 计数 2026-09-14 15:43 复跑为 **20 文件 / 271 测试全绿**（本条撰写时为 261）。

### ✅ 已实现且符合方案

多书数据层（`growth_profiles.books/threads/touch`）；**命题线真在长**（8 用户有 `threads`，evidence 带 `bookId`/`source`/`depth`）；**灯已拆到命题**（`StageLampChart` 按 `topic` + `threadDepth` 渲染）；**书"可见但弱化"**（`/road` 以出处与邀请露出，`productName` 在页面/组件 0 命中）；日频「一段」文案齐（`arcOpen/arcDo/arcClose` + 「今天到这里」+「只看了那一眼也算走完」）；触达七节点 `[3,7,14,21,30,66,90]` + 补发防护；触达禁语写进代码；`consent_records.kind` 分类型且默认值保守。

### ⛔ 待修 / 待验（按级别）

| # | 项 | 证据 | 级别 |
|---|---|---|---|
| 1 | **认知层 L4/L5/M 未实现** | `grep 停顿 src/ content/` = 0 命中 | **必须修**（验证期开跑前；否则只能看行为完成率 → 假结论） |
| 2 | **首启仍是问卷 6 题前置** | 真浏览器：一屏 6 题（含 2 开放题），答完才能继续 | **必须修**（与 §7「第 2 分钟被说中」不符；`<3 分钟` 指标当前不可达） |
| 3 | **邮件从未真发出** | `touchConfigured()`=true（Resend 已配）但 `emailOptIn`=0、`sentNodes` 全空 | **必须验**（真发一封 → 真到达 → 退订可用；另需确认"不回应降频"是否落地） |
| 4 | `docs/06` 3 入口 IA 未实现 | 底部仍 4 Tab（旅程/聊聊/心事/我的） | 排期（本文档 12:47 才补，晚于 M11 范围） |
| 5 | 书的语汇漏进产品层文案 | `arcOpen: "开 · 看见"`；`stageLamp*Hint` 含"松动" | 接第二本前必须修 |
| 6 | `books[]` 全库为空（读时合成） | `books` 数组全为 `[]`，无写入样本 | 补一次"假第二本书"端到端验收 |
| 7 | 产品跑在 **dev 模式**（含 dev tools） | `ps` = `next dev`；页面有 `Open Next.js Dev Tools` | 上线前切 `next build && next start` |
| 8 | `docs/05 §9.3` 枚举名与实现不一致 | `'email_touch'`/`'sensitive_profile'` vs `'touch'`/`'sensitive'` | 建议修 |

### 📌 建议新增（本文件的补充）

**"口径 → 实现位置"对照表**：每条口径一行，标注代码位置与验收方式，下次验收逐条打勾 —— 避免"文档写了、代码没做"重复发生（本轮已发生 2 次：§7 首启路径、本文档 IA）。

---

## 11. 公开环境实测结论（`https://honghong.miller.ink`，2026-09-14）

> 独立验收在**公开地址**上真浏览器走了一遍（落地页 → 首启 → 旅程 → `/road`）。
> ⚠️ 本次在生产库创建了 1 个访客档案（136 → 137），如需清理请示知。

### ✅ 通过

公开地址可用（四语/隐私政策/AI 声明齐）；首启能走完、档案能建；**「我听到的是…」确实存在**（2026-09-14 复核：提交问卷后进入`我听到的是` → `我听到的是：你在这一周里，会想到钱。我理解得对吗？` → `这是你现在拿到的第一个东西。再多说两句，我能看得更准。`；`events.first_echo_shown` 亦有 `source=survey / seconds=23 / under3min=true` 的真实记录）；**日频「一段」确实在一屏内**（`开 · 看见 · 今日一签` + 追问 + `做了吗？记一句感受` + `记下` + `开始今天的对话`）；周期锚点在现场；无红点/未读数/催促文案。

### ⛔ 复核后仍成立（3 条）

| # | 问题 | 证据（复核方式：JS 读真实 DOM 文本） | 级别 |
|---|---|---|---|
| 12 | 四个阶段（1 看见 / 2 松动 / 3 练习 / 4 活法）仍是**旅程页主视觉**，并标注"参考节奏约 3 周" | `四个阶段，不设期限。走到哪一步，由你说了算。` + `1 · 看见 / 参考节奏约 3 周` | 排期（决策 #30：升格为用户坐标 + 命名随书） |
| 13 | `开 · 看见` 在真界面渲染（书的语汇进产品层） | 旅程页正文 `开 · 看见 · 今日一签`（与 `arcOpen` 一致） | 接第二本前必须修 |
| 14 | 「写信」在旅程页**常驻**（本文主张移到机关节点） | 旅程页独立块：`写一封信 / 写信` | **待用户拍板**（与 `docs/02 §11` MVP 表述的冲突已实证） |

### ❌ 撤销：以下 3 条是本轮误报（2026-09-14 复核）

> **不要照修。** 本轮走查用的是坐标点击 + 长截图，这两者各自产出了误报。

| 原 # | 当时结论 | 复核结果 | 误报原因 |
|---|---|---|---|
| 9 | 首启没经过「我听到的是…」 | **不成立。** 提交问卷后确实进入该屏（文案见上）。代码路径 `submitSurvey → setStep('echo') → /api/onboarding/reflect` 与实测一致；后端 `events` 里该事件今日 10 条 `source=talk` + 1 条 `source=survey` | 当时提交按钮位于**视口之外**（`rect.top=830 > 视口高 633`），坐标点击**根本没点到按钮**；随后那次点击落在页面别处 → 误读成"提交后直接进旅程"。页面此时可滚动（文档高 1010），按钮可达 |
| 10 | 首启页主导航与表单重叠、盖住第 5 题 | **不成立。** `nav` 为 `position: static`，按文档流排在 `main` **之后**，与表单无重叠；`main` 高 878、`nav` 起于文档末尾 | 依据**长截图**判断布局。长截图对按视口高度（`100dvh` flex 容器）排布的页面会产出"导航浮在内容中间"的假象 —— 与 `docs/07 §6.2` 已登记的同类假象是**同一个坑** |
| 11 | 已体检访客访问根路径仍显示落地页 | **撤回。** `src/app/[locale]/page.tsx` 的重定向条件为 `profile?.portrait` 存在（注释明确"仅有档案行不算——如只在旅程页记过一条记录但没体检的访客，仍应看到体检入口"）。本轮访客没有生成画像（中途离开），落地页属**预期行为** | 没先读代码注释就下结论。至于"已有画像的回访者是否真被重定向到 `/journey`"**仍未实测** → 降级为**待验**，不算问题 |

### 由此更新的优先级

**P0（验证期开跑前必须完成）**：

1. **认知层三指标零成本化**（L5＝「合」第③问；L4＝AI 识别后一键记、可撤销；M＝自动算）
2. **邮件真跑通 + 可归因**（真到达；加 open/click 或回访来源标记；模板随用户状态）
3. **退订 GET → 确认页，POST 才执行**（避免邮件预扫描误退订）
4. **首启重排**（首屏不出现问卷、先给一件小事；问卷拆进对话）

**原 P0 第 5 条「被说中这一屏」已撤销** —— 复核证实该屏已存在且文案正确（见上），
只需在首启重排时**一并检查顺序与文案**，不需要新建这一屏。

### 附：本轮的验收方法教训（写进验收纪律）

线上验收**必须用 JS 驱动真实 DOM**：滚动到元素 → `element.click()` → 读 `innerText` 判定。
**不得依赖坐标点击与长截图**判断交互与布局 ——
坐标点击在元素位于视口外时**静默失效**（不报错、不滚动，看起来像"点了没反应"）；
长截图会破坏按视口高度排布的容器，产出"重叠 / 悬浮 / 盖住"一类的假象。
本轮 3 条误报中，2 条来自这两点。反之，**读 DOM 文本做结论**的项（#12/#13/#14、`docs/07 §6.2` 各条）全部经得起复核。
