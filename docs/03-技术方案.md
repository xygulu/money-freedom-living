# 03 · 技术方案

> 状态：v1（2026-09-11）。产品依据见 [02-产品方案.md](02-产品方案.md)（下称 P§n）。
> 本文档是实施图纸 + 决策映射；写代码前先读 AGENTS.md 的 Next.js 16 破坏性变更警告。

---

## 1. 技术栈（对齐 saas-kit 已验证组合）

| 层 | 选型 | 版本/说明 |
|---|---|---|
| 框架 | Next.js App Router | **16.3.3**（saas-kit 验证版；注意 AGENTS.md 破坏性变更警告） |
| UI | React 19.2.8 + Tailwind CSS v4 | pnpm 强制 |
| DB | Neon Postgres | `@neondatabase/serverless` 1.1.0 双驱动（saas-kit db.ts 原样接入） |
| Auth | Better Auth 1.7.x | saas-kit auth.ts；MVP 仅邮箱密码，凭证缺失的社交登录自动不启用 |
| 支付 | saas-kit payments 层 | Creem adapter 单通道（P§7） |
| 配额 | saas-kit quota | 游客匿名 cookie / 免费 3 会话 / VIP 无限（P§7） |
| LLM | Anthropic 兼容网关 + failover | 移植哄哄 `llm-providers.ts` 模式；智谱 glm-5.3-flash 起步，LLM_PROVIDER_2_* 预留（P§1 Go/No-Go：盲测不过即切换） |
| i18n | **自研轻量** `[locale]` 路由段 | messages JSON + getDict + middleware 检测；不引第三方框架（零兼容风险，四语够用） |
| 部署 | Vercel + Neon | 海外区域；域名/品牌名待定（P§1 里程碑 5） |

**不用**：重 UI 框架库（组件手写，设计走 frontend-design skill 流程）、ORM（saas-kit 是原生 SQL 风格，保持一致）。

## 2. 目录结构

```
money-freedom-living/
├── content/                          # 内容层（P§10），git 管理
│   ├── books/<bookId>/<locale>/{journey,daily}/   # ★M11-B 书是一级维度（当前 books/money-freedom/{en,zh-CN}/）
│   ├── safety/{keywords,resources}/<locale>.json  # 危机词表 + 转介资源（全产品级，不分书）
│   └── practices/                    # 中文单一事实源（frontmatter 带 bookId）
├── deploy/                           # ★M11-E：mfl-touch.{service,timer} + README
├── scripts/                          # 18 个：build-content / schema-migrate / smoke-m3…m11 / m8-* / touch-* / repro-*
├── src/
│   ├── proxy.ts                      # ★Next 16 起 middleware 改名 proxy：locale 检测/重定向（Accept-Language → cookie）
│   ├── generated/content.json        # build-content 的构建期产物（content.ts 只读它，不读运行时文件）
│   ├── config/db-providers.ts        # saas-kit
│   ├── i18n/
│   │   ├── config.ts                 # visibleLocales 四项；enabledLocales=['en','zh-CN']；defaultLocale: en
│   │   ├── messages/{en,zh-CN,zh-TW,ja}.json
│   │   └── get-dict.ts
│   ├── app/
│   │   ├── [locale]/
│   │   │   ├── page.tsx              # 落地页（已有画像者重定向）
│   │   │   ├── onboarding/           # 体检向导（问卷→我听到的是…→初谈→同意→画像→校准）
│   │   │   ├── journey/  journey/changes/    # 「一段」；阶段回望
│   │   │   ├── road/                 # ★「你走过的路」：书唯一露出处 + 命题
│   │   │   ├── chat/  chat/history/[sessionId]/
│   │   │   ├── journal/  letters/  timeline/  portrait/  portrait/compare/
│   │   │   ├── vip/  me/  login/  recover/  unsubscribe/  privacy/
│   │   │   └── layout.tsx            # 底部 Tab 框架（当前 4 个）
│   │   └── api/
│   │       ├── auth/[...all]/        # saas-kit 托管
│   │       ├── quota/{route,consume} # saas-kit（kind=chat_session）
│   │       ├── payments/*            # saas-kit
│   │       ├── onboarding/{answers,talk,reflect,portrait,calibrate}/
│   │       ├── chat/sessions/route.ts        # 建会话（配额：首回复成功后落账）
│   │       ├── chat/[sessionId]/route.ts     # 消息（SSE 流式）
│   │       ├── journal/{route,[id]/reply}/
│   │       ├── letters/route.ts
│   │       ├── journey/{anchor,assess,changes,experiment}/   # ★experiment 也是安全层入口（§6）
│   │       ├── portrait/evolve/route.ts
│   │       ├── touch/{consent,dispatch,preview,unsubscribe}/ # ★M11-E 触达层
│   │       └── me/{export,delete,recovery/generate,recovery/recover}/
│   ├── lib/                          # 37 个。saas-kit 原样：db/auth/auth-client/password/validation/
│   │   │                             #   entitlements/quota/quota-client/payments/*
│   │   ├── llm.ts                    # provider failover（移植哄哄）+ 流式调用
│   │   ├── prompt.ts                 # 上下文组装管线（§5）
│   │   ├── safety.ts                 # 危机识别（§6）
│   │   ├── content.ts                # 读 generated/content.json
│   │   ├── profile.ts                # 成长档案读写（§4）
│   │   ├── stage.ts                  # ★灯判定表：BOOK_LAMPS 按 bookId 索引 + LampRule.topic
│   │   ├── road.ts                   # ★「你走过的路」数据
│   │   ├── touch.ts                  # ★触达：节点选择 / 信件组装 / 退订链接 / 可投递性
│   │   ├── consent.ts                # ★同意（kind='sensitive' | 'touch'）
│   │   ├── analytics.ts              # ★events 埋点
│   │   ├── email-resend.ts           # ★Resend 发信（触达 + 验证/重置）
│   │   ├── memory.ts                 # 同步轻摘要 / 异步详摘要 / 滚动合并
│   │   ├── assess.ts evolution.ts changes.ts timeline.ts anchor.ts letters.ts journal.ts
│   │   ├── receipt.ts sse.ts privacy.ts migrate.ts recovery.ts onboarding.ts
│   │   └── identity.ts               # user 或 guestKey 统一解析（saas-kit 游客身份复用）
│   ├── components/                   # 20 个手写组件
│   └── tests/                        # vitest：20 文件 / 261 测试
```

## 3. 数据库表（saas-kit 之外的新增）

幂等建表走 saas-kit `ensureSchema` 模式，全部新表如下：

```sql
-- 成长档案（P§6）：一行一用户，JSON 字段整体读写
growth_profiles (
  user_key TEXT PRIMARY KEY,          -- userId 或 guestKey（identity.ts 统一解析）
  locale TEXT NOT NULL DEFAULT 'en',
  portrait JSONB,                     -- {spoken[], baseColor, moments[], inferred{script,status,pending},
                                      --  toFuture, version, calibrations[]}
  concerns JSONB DEFAULT '[]',        -- [{content,status,sourceAt}]
  stage SMALLINT DEFAULT 1, stage_started_at TIMESTAMPTZ DEFAULT now(),
  pinned JSONB DEFAULT '[]',          -- [{kind: taboo|promise|open, text, createdAt}]
  memories JSONB DEFAULT '[]',        -- [{date,summary}]（滚动合并层）
  experiments JSONB DEFAULT '[]',     -- [{date,action,feeling}]
  letters JSONB DEFAULT '[]',         -- [{stage,content,state,aiReply,createdAt}]
  stamps JSONB DEFAULT '[]',
  payday JSONB,                       -- {type: monthly|biweekly|weekly, day?}；null=未设不显示锚点
  recovery_code_hash TEXT,            -- 加盐哈希（P§7）
  total_active_days INT DEFAULT 0, last_active_date DATE,
  created_at/updated_at TIMESTAMPTZ
)

-- 对话（量大，独立表；会话内 ≤20 轮，P§7）
chat_sessions (id UUID PK, user_key, locale, kind TEXT DEFAULT 'chat',   -- chat|onboarding_talk
  message_count INT DEFAULT 0, quota_consumed BOOL DEFAULT false, status TEXT, created_at)
chat_messages (id BIGSERIAL PK, session_id UUID, role TEXT, content TEXT, created_at)

-- 日记（免费自己写，VIP AI 回应，P§7）
journal_entries (id BIGSERIAL PK, user_key, locale, content TEXT,
  ai_reply TEXT, replied_at TIMESTAMPTZ, created_at)

-- 安全事件（只存引用与类别，不存原文，P§9 日志纪律）
-- source 真取值（实读五处调用点，2026-09-14 复核）：
--   chat      src/app/api/chat/[sessionId]/route.ts:175
--   journal   src/app/api/journal/route.ts:29
--   letter    src/app/api/letters/route.ts:31
--   experiment src/app/api/journey/experiment/route.ts:38
--   calibrate src/app/api/onboarding/calibrate/route.ts:39
-- category 真取值：crisis | domestic_violence（src/lib/safety.ts:11 SafetyCategory）
safety_events (id BIGSERIAL PK, user_key, source TEXT,   -- chat|journal|letter|experiment|calibrate
  category TEXT, handled BOOL DEFAULT false, created_at) -- category: crisis|domestic_violence

-- 画像版本快照（M9）：收录所有版本含当前版；growth_profiles.portrait 仍是当前版
-- 反规范化副本。UNIQUE(user_key, version) 支撑懒回填/并发演进幂等（ON CONFLICT DO NOTHING）
portrait_versions (id BIGSERIAL PK, user_key, version INT,
  portrait JSONB,                        -- 完整 Portrait 快照（含当时 calibrations/scriptStatus）
  source TEXT,                           -- onboarding | evolve | backfill
  material JSONB DEFAULT '{}',           -- 该版基于的新素材量 {memories,journals,experiments,daysSince}
  created_at TIMESTAMPTZ, UNIQUE (user_key, version))

-- M9 列变更（幂等追加）：
--   growth_profiles + portrait_evolution JSONB DEFAULT '{}'  -- 演进状态
--     {dismissedAt, lastGeneratedAt, proposedSeenAt, generatingAt}；只存用户动作与锁，
--     提议与否读时纯函数推导（自愈）。⚠ 不能放 portrait JSONB 里——校准路由整体覆盖会抹掉
--   growth_profiles.stamps 启用                              -- [{kind, earnedAt}]，按 kind 幂等。
--     M9 修订后灯的展示真值是 stage_assessment.confirmed.lamps（评估结论：亮/不亮
--     + 依据，done = 该评估说亮才亮）；stamps 退为确认仪式同步的心印存档
--     （足迹/心印陈列），不再参与灯判定（存量机械印同样不参与）
--   growth_profiles + stage_assessment JSONB DEFAULT '{}'    -- 阶段评估状态（M9 修订）
--     {pending, confirmed, confirmedAt, dismissedAt, generatingAt, proposedSeenAt}；
--     pending/confirmed 为完整评估 {actualStage, lamps:[{kind,lit,evidence}], summary, nextHint, assessedAt}；
--     同 portrait_evolution：只存用户动作/锁/结果，「是否该提议」读时纯函数推导；
--     confirmed 是证据的长期居所 + 下次评估的素材基线（confirmedAt）。在行内 → 导出/删除/游客迁移天然覆盖
--   chat_sessions + closed_at TIMESTAMPTZ                    -- 封存时刻；存量 NULL 用 COALESCE 兜底

-- M7 / 安全 列变更（幂等追加；原 §3 漏记，2026-09-14 补）：
--   growth_profiles + daily_seen JSONB DEFAULT '[]'          -- 今日一签去重（上限 90 条 ≈ 90 天不重复）
--   chat_sessions   + safety_flagged BOOLEAN NOT NULL DEFAULT false   -- 命中危机信号的会话

-- 敏感信息单独同意（P§9）+ M11-E 触达同意（原 §3 漏记，2026-09-14 补）
consent_records (id BIGSERIAL PK, user_key TEXT NOT NULL,
  granted BOOLEAN NOT NULL,
  policy_version TEXT NOT NULL,           -- lib/consent.ts POLICY_VERSION；文案实质修订时递增
  ip_hash TEXT,                           -- sha256(ip + RECOVERY_PEPPER)：能判重/审计，还原不出 IP
  kind TEXT NOT NULL DEFAULT 'sensitive', -- ★M11-E：'sensitive'（画像与深谈）| 'touch'（触达 opt-in）
                                          --   默认值取保守方向：漏传 kind 只会退回旧行为，不会被误记成"已同意收邮件"
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  INDEX idx_consent_user_kind (user_key, kind, created_at DESC))

-- 匿名埋点（P§11 验收指标的数据底座；只存元数据，绝不含用户原文）
events (id BIGSERIAL PK, user_key, name TEXT, locale TEXT,
  metadata JSONB,                         -- 数值/枚举/桶，不放用户文本
  created_at TIMESTAMPTZ)

-- 恢复码限速与审计（P§7）：只存 scope/主体哈希/结果，不存尝试码本身
recovery_attempts (id BIGSERIAL PK,
  scope TEXT NOT NULL,                    -- 'account' | 'ip' | 'global'
  subject TEXT NOT NULL,                  -- username / ip_hash / '*'
  ok BOOLEAN NOT NULL, created_at TIMESTAMPTZ)

-- M11-A 多书架构列变更（幂等追加；设计见 docs/05 §4，改表必须同步本节）：
--   growth_profiles + books   JSONB DEFAULT '[]'
--     [{bookId, status:'active'|'paused'|'done', startedAt, stage, stageStartedAt, finishedAt?}]
--     当前书 = status='active' 那条；存量/新行读时补 DEFAULT_BOOK_ID('money-freedom')，不写迁移脚本
--   growth_profiles + threads JSONB DEFAULT '{}'
--     命题线：{[topicId]: {firstSeenAt, lastSeenAt, depth:'seen'|'replaced'|'mastered',
--                          evidence:[{at, bookId, source, quote, ref}]}}
--     source: chat|journal|letter|experiment|assessment；quote 必须是用户原话
--   growth_profiles + touch   JSONB DEFAULT '{}'
--     触达状态：{emailOptIn, optInAt, unsubToken, lastSentAt, sentNodes:{D7:at,…}, lastOpenedAt?}
--     sentNodes 保证每个节点一辈子只发一次（claimTouchNode 原子占位）
--   portrait_versions + bookId TEXT NULL        -- 触发这一版演进的书；UNIQUE(user_key,version) 不变
--   portrait_versions.source 枚举 + book_switch
--   六个数组元素 + bookId?（缺字段=默认书，向后兼容，不迁移）：memories / experiments / letters /
--     stamps / dailySeen / concerns
--
-- 纪律：以上全部在 growth_profiles / portrait_versions 行内 → 导出 / 删除 / 游客迁移天然覆盖。
--   ⚠️ 另建表就要手工补 lib/migrate.ts 白名单 + me/export + me/delete 三处，必然漏。
```

纪律：对话原文只在 chat_messages（用户可删）；`safety_events`/日志不含原文；删除账号时全部级联（P§9）。

## 4. 身份与档案

- `identity.ts`：`getKey()` → 登录用户用 `user.id`；游客用 saas-kit 匿名 cookie（IP 兜底），key 前缀 `g:` / `u:` 区分。注册后迁移：`UPDATE ... SET user_key=u:xxx WHERE user_key=g:xxx`（同事务换主键）
- 恢复码：12 位（去易混字符 `23456789ABCDEFGHJKMNPQRSTUVWXYZ`），生成时存 `sha256(salt+code)`；`POST /api/me/recovery` 验证 = 账号级 5 次/时 + IP 级 20 次/时 + 全局失败锁定
- 活跃足迹：任一动作（会话/微行动/日记）完成时 `total_active_days` 原子 +1（`last_active_date != today` 时），单条 UPDATE 带条件

## 5. LLM 上下文组装（P§6 优先级的落地）

`prompt.ts: buildChatContext({ locale, profile, stage, sessions })` 按优先级装配，token 预算内从低优先级开始裁剪：

```
0. **LANGUAGE_RULES**（★M8 追加；**仅次于安全规则**：四语强制输出语言，声明"指令本身是中文不构成用中文回复的理由"）
1. 安全规则层（固定，四语版；危机协议见 §6；"内容由 AI 生成"声明）
2. pinned：用户禁忌/重要承诺（超预算也不裁）
3. 阶段引导：**`content/books/{bookId}/{locale}/journey/N.md`** 正文 + ai_stance
4. practices：`content/practices/` 按 stage **+ ★bookId** 过滤 → 中文原文注入 + 指令
   "以下为创造者的实践经验（原文中文），用 {locale} 转述其经验，标注为创造者观点"
5. 画像：spoken 原话引用 / 已确认脚本 / 底色 / moments
6. **★命题线 `threads`**：跨书累计的程度（`depth`）与原话（`evidence[].quote`）—— **多书"交叉印证"的唯一数据源**（docs/05 §3.3）
7. 开放 concerns
8. 最近 N 轮对话（chat_messages）
9. 远期 memories（预算有余才放）
```

- 初谈（onboarding_talk）单独 system：只深挖一个点、不建议不纠正，结束后产出 JSON（瞬间候选/脚本候选）供画像生成
- 画像生成：`POST /api/onboarding/portrait` 一次性生成四段 + 空泛标签禁令写进 system
- 记忆：会话结束（轮数上限/用户离开）触发同步轻摘要（1-2 句，同请求内完成）；详摘要走 Vercel 后台函数或下次会话前惰性补（MVP 用惰性，不上队列）

## 6. 安全层（P§8 的实现）

- `safety.ts`：两段式——① 关键词粗筛（每语言词表，**`content/safety/keywords/{locale}.json`**，git 管理）② 命中后 LLM 语义确认（低温度、仅分类：危机/家暴/无，不产生面向用户文本）。抽检集 `src/tests/safety-fixtures/{locale}.json` 进 CI
- 命中处理：对话流插入转介消息（该语言资源表 `content/safety/resources/{locale}.json`）+ `safety_events` 落一行 + 该会话后续 system 注入"稳定陪伴模式"（不深挖、不拉回任务）
- 覆盖入口（★2026-09-14 补第 5 个）：chat / journal（VIP 回应前）/ letters（回信前）/ calibrate 反馈 / **`journey/experiment`**
  ⚠️ 最后一个容易漏：M11-D 的微行动记录与「合」的"实际发生了什么 + 一句感受"**走的就是这个入口**；漏了它，会误以为"新入口没过安全层"
- 每语言抽检召回率达标才开该语言（P§1 语言开放策略的实现依据）

## 7. 配额落账（P§7"失败不扣"的实现）

```
POST /api/chat/sessions     → 建 session（quota_consumed=false），前端进入
POST /api/chat/[id] 消息流式 → AI 首条回复完成后：
  若 !quota_consumed → /api/quota/consume(kind=chat_session) + quota_consumed=true（同事务语义：先 consume 成功才置位）
  失败/中断 → 不落账，session 可重试
轮数上限 20：message_count ≥ 40（=20 轮）时 API 返回温和收尾提示
游客/免费/VIP 档位由 saas-kit quota 层判定；VIP 跳过 consume
```

## 8. i18n 实现

- **`proxy.ts`**（★Next 16 起 `middleware.ts` 已改名 proxy）：无 locale 前缀 → 读 cookie `locale` → Accept-Language 匹配（zh-* → zh-CN/zh-TW 分流、ja、en）→ 302 到 `/{locale}/...`
- `getDict(locale)` 读 messages JSON（`import` 静态引入，构建期内联）；**ja/zh-TW 的 messages 完整就绪但选择器锁定 Coming soon**（`config.ts` 中 `enabledLocales = ['en','zh-CN']`，`visibleLocales` 四项——开关一处改。⚠️ 改 `enabledLocales` 后 dev server 需重启，生产 build 无此问题）
- 内容装载：`content.ts` **只读构建期产物 `src/generated/content.json`**，由 **`scripts/build-content.mjs`** 从 `content/books/{bookId}/{locale}/…` 生成（`pnpm build-content`；`pnpm check-content` = build-content + `vitest content`）。
  ⚠️ 原写"脚本 `scripts/check-content.mjs` 输出各语言就绪度"—— **该脚本不存在**（2026-09-14 核实）。**ja/zh-TW 文件存在性**是解锁检查项之一，由 `build-content` 的产物体现。

## 9. 环境变量（★2026-09-14 按代码实读校正）

| 组 | 变量 | 备注 |
|---|---|---|
| 数据库 | `DATABASE_URL`（+ `DATABASE_URL_UNPOOLED` / `DATABASE_URL_FALLBACK` / `DATABASE_URL_3`） | saas-kit |
| 认证 | `BETTER_AUTH_SECRET`、`BETTER_AUTH_URL` | `BETTER_AUTH_URL` 部署必须设真实域名，否则认证端点 Origin 校验 403 |
| 社交登录（可选） | `GOOGLE_CLIENT_ID/SECRET`、`GITHUB_CLIENT_ID/SECRET` | 不配则该 provider 不启用 |
| 邮件 | `RESEND_API_KEY`、**`RESEND_FROM`** | `touchConfigured()` 同时看这两个；缺任一 → 触达**优雅降级成 dry-run**（不报错、不假装发了） |
| 支付 | `CREEM_API_KEY`、`CREEM_PRODUCT_ID`、`CREEM_WEBHOOK_SECRET` | fail-closed：key 与 webhook secret 必须同时配 |
| LLM | `ANTHROPIC_BASE_URL` / `ANTHROPIC_AUTH_TOKEN` / `ANTHROPIC_MODEL` + `LLM_PROVIDER_2_*` | provider failover |
| **触达** | **`TOUCH_CRON_SECRET`** | ⚠️ **没配 → `/api/touch/dispatch` 直接 503**（绝不开一个谁都能打的发信端点） |
| 站点 | **`NEXT_PUBLIC_BASE_URL`** | ★原写的是 `NEXT_PUBLIC_SITE_URL`，**代码实际读 `NEXT_PUBLIC_BASE_URL`**；触达回程链接与退订链接都靠它 |
| 其它 | `RECOVERY_PEPPER`（恢复码盐）、`SUPPORT_EMAIL`（/privacy 展示）、`APP_NAME`（可选展示名）、`TRUSTED_ORIGINS` | |
| 脚本用 | `SMOKE_BASE`、`TOUCH_BASE_URL` | `scripts/*` |

> ⚠️ **连带缺口（→ `docs/08-待修清单.md` P1-2）**：`.env.example` 里**缺 `TOUCH_CRON_SECRET` / `NEXT_PUBLIC_BASE_URL` / `TRUSTED_ORIGINS`** —— **新部署的人不会配 `TOUCH_CRON_SECRET`，dispatch 会直接 503**。

## 10. 实施里程碑

| # | 内容 | 验收 |
|---|---|---|
| M1 | 脚手架 + saas-kit 全量接入 + [locale] 路由 + middleware | `tsc --noEmit` 0 错；`pnpm build` 过；/api/quota 冒烟通过；四语首页切换 |
| M2 | content/ 结构 + 四阶段内容 v1（en+zh-CN）+ 一签 90 条机制 | check-content 全绿；journey 页渲染 |
| M3 | 体检全流程（问卷→初谈→确认→画像→校准）+ 档案落库 | 走完流程 portrait 有四段；校准写回 |
| M4 | 对话（会话配额落账 + 双层记忆 + 安全层 + pinned） | 失败不扣；第二天对话引用昨日摘要；危机词触发转介 |
| M5 | 旅程主页（今日三件事）+ 日记 + 信件闭环 | 微行动写入 experiments；信件回信不分析 |
| M6 | VIP（Creem checkout→verify）+ VIP 功能门禁 | 免费墙三处触发；VIP 无限+回应+报告占位 |
| M7 | 锚点/归来问候/埋点/隐私政策与 AI 声明四语/导出删除 | 合规清单（P§9）逐项打勾 |
| M8 | **Go/No-Go**：LLM en/ja 盲测、危机抽检、单位经济测算、Creem 确认 | 决定上线语言与参数 |
| M9 | 旅程整合（timeline/聊天回看/「旅程中的我」）+ 阶段进度与心印 + 画像演进与对比；修订：进度改认知/行为评估制（/api/journey/assess 四 action）+ 全局进度条 | `tsc` 0 错；125 单测全绿；`scripts/smoke-m9.mjs` 全通过（六类节点/越权 404/渲染不发印/评估提议闸/并发恰一次真生成/confirm-advance-dismiss 流/导出含 stamps+assessment/级联删除） |
| M10 | 机制验证修订：读→做→证→叙的落地引擎化（微行动**即时见证回应** + 涉钱动作 **0 元替代版** + 机制验证仪表埋点） | 已实施；产品口径见 docs/02 §5/§10/§11/§12，技术细节见 §11 修订记录 |
| **M11** | **多书架构与触达层**：成长归用户 / 书是内容层 / **命题线是桥** + 上手路径前置 + 日频「三件事 → 一段」+ 邮件触达层 | **A/B/C/D/E 全部已实现**（实现进度与提交号见 `docs/05 §0.1`）；验收含本项目的关键一条：**存量单书用户升级后档案 diff 只多 `books`/`threads`/`touch` 三键，别处零差异** |

> **里程碑表的时态只到「已提交」为止。** 当前进行中的项见 `docs/05 §0.1`（实现进度）与 `docs/08-待修清单.md`（未完成项）——本表不再逐条追。

## 11. 修订记录

### 2026-09-14 · P0-3 首启重排：welcome → talk → echo → form(分段) → reflect；问卷不再前置、同意顺序未动；`/api/onboarding/talk` 与 `/api/chat/[sessionId]` 取消 questionnaire_required 400，`/api/onboarding/reflect` 新增 mode='echo'（素材=对话）；i18n 四语增 `onboarding.welcome` / `onboarding.form`；新增 `scripts/verify-p0-3.mjs`（真浏览器读 DOM + events.first_echo_shown.seconds<180）；`scripts/smoke-m11.mjs §⑤` 与 `scripts/repro-ux.mjs` 同步更新
### 2026-09-14 · 技术方案一致性校正：§2/§3/§5/§6/§8/§9 共 27 处漂移清掉（明细见 `docs/09-技术方案一致性对照.md`）；同日 §3 安全事件枚举复核修正、§10 里程碑表时态校正
### 2026-09-14 · M11-A/B：多书架构数据模型 + 内容层书维度（`books`/`threads`/`touch` + `bookId` + `consent.kind`，**零新表**）｜`87d53da`
### 2026-09-14 · M11-C：上手路径前置——问卷交完先给「我听到的是…」，同意挪到被说中之后｜`b598ca0`
### 2026-09-14 · M11-D：日频形态「三件事 → 一段」+ 进度条改区域视图 + 「你走过的路」｜`96356b0`
### 2026-09-14 · M11-E：邮件触达层——调度端点 + 七节点选人 + 四语模板 + opt-in/退订 + Resend + 保留域名硬闸 + 预览端点｜`c7a3375` `fd14987`
### 2026-09-14 · 触达层按验收报告补齐 7 项（MIN_GAP_DAYS=4、D3 门槛、最近引用、退订 GET/POST 分离、RFC 8058、占位失败回滚、回程归因）｜`2b5c479`
### 2026-09-14 · 段数与灯改为跟着当前在读的书走：新增 `stageCountFor(bookId)`，`MAX_STAGE` 封存、四个调用点全部改为按书取｜`87b5245`
### 2026-09-14 · 认知层机制级改动「灯不靠嘴说」：需真实行为证据的灯，没记录就不点亮｜`bab0745`
### 2026-09-14 · AI 与日界线跟随用户所在时区｜`baa5922`
### 2026-09-14 · 文案与交互修正批次：陪伴者自称统一「我」、变化清单/评估报告钉死第二人称、变化清单「下一步」按钮化、会话摘要改第二人称、旅程页「随便聊聊」与 `?start=1` 一次点击即开聊｜`27b217c` `89d26a5` `e1f2d31` `7c9171f` `13a5e9b` `dc17554`
### 2026-09-14 · M11 验收：`docs/06`（信息架构与认知层）+ `docs/07`（M11 验收报告）+ `docs/08`（待修清单）+ `docs/09`（一致性对照）建立并纳入 git｜`86a2771` `1651fc9` `b2f5c70`
### 2026-09-14 · 前台改造 70-2（一幕·一人·一存档）：版本策略 `ui_version` cookie（`new` 默认 + `classic` 兼容过渡，废除判据另起 P3 批次）；`/journey` 4 行守卫保留为经典版路径；新增 `/journey-new`（DailyCardNew + ActCard + MirrorCard + PathBar 五段）与 `/archive`（ArchiveList + VersionSwitch + ThemeSwitch）；移除 layout 全局 4 Tab + 引入 `<Companion />`（右下角 54×54，三态：静/晃/开，两入口：球静默 + 门引用用户原话）；陪伴者 nudge 生成器（`src/lib/companion-nudge.ts`，L1 模板优先 → L2 LLM 降级 → L3 兜底 + 红线正则）；新增 `companion_nudge_events` + `ui_version_view_events` 两表 + `POST /api/companion/nudge` + `POST /api/ui-version`；i18n 四语新增 `oneScreen/companion/newJourney/archivePage/nudge`；新增 `scripts/verify-stage.mjs`（一屏放下 + 红线自检 + 经典/新版切换 + nudge API + ui-version API）；新增 `src/tests/companion-nudge.test.ts`（20 用例）。**§6 nudge 文案语义按用户 2026-09-14 意图纠偏，偏离已登记 `docs/08`**

### 2026-09-14 · /archive 回到旅程导航 bugfix：archive 页**原本没有"回到旅程"入口**（用户只能浏览器后退）；新增显式链接并按 ui_version 智能分流 —— `new → /journey-new`、`classic → /journey`，避免链接走 `/journey` 触发守卫反向 redirect，给人"返回到旧版"的错觉。i18n 四语增 `archivePage.backToJourney`；`scripts/verify-stage.mjs` 加 2 条断言（new 指向 `/journey-new`、classic 指向 `/journey`）。**触发：用户 2026-09-14 反馈"在存档页点击回到旅程返回到旧版页面了"**；根因 + 数据落点登记 `docs/08-待修清单.md` 附E-4。tsc 0 错 + 322 单测全过 + verify-stage 全过。

### 2026-09-14 · 70-2 加固：按原型回归 15 处偏离 + 版本隔离 + 设置登出入口

- **触发**：用户 2026-09-14 反馈"你有很多细节没有按照原型来实现，请仔细检查下完全遵照原型来实现" + 用户拍板两点（邮件回程默认 `/journey-new`、登出需二次确认）。
- **根因 1（§6 例外误禁）**：`companion-nudge.ts` 红线正则误禁了用户拍板保留的「今天这一步你还没做」（§6 唯一例外条款）；`CompanionChat` 没实现 `hintToday` 模式。这是 §6 唯一例外条款的**违反**。
- **根因 2（触发素材缺失）**：§6 三种合法触发 ② 日历（发薪日 / 月末 / 大促）缺失，只有 L1a/b/c 三档。
- **根因 3（UI 偏离原型）**：topbar 用文字链接、SVG path 与原型不一致、act 三选一错位、"合"做成页内表单而非 commitSheet 弹层、PathBar 4 节点硬编码跨语种 bug、MirrorCard echo 走 close API 而非前端分支、archive 缺 5 行、archive 顶部按钮文案错位。
- **根因 4（版本不隔离）**：8 处 server page `Link` 硬编码 `/${locale}/journey`，切换 ui_version 后导航跨版本跳转。
- **根因 5（缺登出）**：archive 第三段「设置与账号」下无登出入口；MeAccount 顶部也无。
- **修复（原型回归 A–O 共 15 处）**：
  - A. §6 例外：`hintToday` 模式四件套（title/sub/bubble/foot）；红线正则移除 `/还没做/` `/今天你还没/` `/你还没/`，**保留**其他完成度指责红线
  - B. L1d anchor 触发（payday / month_end / sale 三种）
  - C. topbar 改 SVG 存档图标按钮（24×20 viewBox）
  - D. archive 顶部按钮文案改"回到今天这一步"（四语）
  - E. archive 补 4 行（写给以后的我 + 账号段下邮件与通知 / 导出·删除 / 语言·恢复码·会员）
  - F. ActCard 三选一改"做了 / 换一个 / 跳过"（原 commitSheet Q1 三档降级到 sheet 内）
  - G. "合"改成 commitSheet 弹层（与 chatSheet 同级 sheet）
  - H. PathBar 4 节点改 `<span>`（不是 Link）；删 `dictStageLocale()` 死代码
  - I. PathBar hint 文案动态生成（`${n} 格亮着。这里不显示百分比…`）
  - J. MirrorCard echo 走前端分支硬编码（不调 close API）
  - K. 陪伴者 SVG path 替换为原型 `d="M12 4.6c-2.6 0..."` + `d="M9.6 4.2c..."`
  - L. Companion ball 加 `.pulse` 呼吸圈 + box-shadow 三层照原型
  - M. act 选中后不直接提交：①"做了"→commitSheet；②"换一个"→切 act 文本；③"跳过"→折叠 act 段
  - N. NewJourneyView `.path` 加 `margin-top:auto` 一屏放下硬约束
  - O. （无回归，已合规）
- **修复（版本隔离）**：`ui-version.ts` 加 `journeyHrefFor()` + `withJourneyHref()` helper；i18n 四语加 `nav.journeyHref: ""` 占位；8 处 server page `Link` 全替换；`VersionSwitch` 接收 `journeyHrefByVersion` prop；`touch.ts` 邮件回程改 `/journey-new`（保留 `from=touch&node=` 参数）。
- **修复（登出）**：新增 `SignOutButton`（带二次确认）；archive 第三段登录态挂入口 + `MeAccount` 顶部各一份；i18n 四语增 `me.signOut` + `me.signOutConfirm`。
- **新增**：`<SignOutButton />` + `src/tests/ui-version.test.ts`（15 用例：`journeyHrefFor` × 3 + `withJourneyHref` × 12）。
- **测试更新**：`src/tests/companion-nudge.test.ts` §6 例外文案反向断言 + L1d anchor 用例 + `source` 枚举加 `template-anchor`。
- **verify-stage 新增 8 条断言**：topbar SVG / 陪伴者 SVG path / .pulse / PathBar hint 动态生成 / 4 阶段非 Link / archive 游客态无 SignOut / archive 账号段 3 行 / 顶部按钮文案"今天"。
- **验证**：tsc 0 错 + **341 单测全过**（含 15 ui-version 新用例）+ verify-stage 全过。根因 + 数据落点登记 `docs/08-待修清单.md` 附E-5。

### 2026-09-15 · 访客分级：金钱关系测试 only + 首页钩子 + 部分画像 → 注册

- **触发**：用户 2026-09-14 反馈两条——①"没有登录的用户只能做金钱关系测试，可以查看测试结果的一部分，要看全部需要引导注册或登录"；②"用户访问的首页（落地页）需要有一句话引导，引起用户使用兴趣，金钱关系测试的结果也一样需要能引起用户兴趣，引导用户注册登录"。
- **根因**：现状对访客**半开半放**——`docs/02 §7` capability matrix 把访客当成"准用户"（可聊天 1/天、可日记、可写信、可读画像全文）。落地页只有一个 CTA（→ /onboarding），没有登录注册入口。访客走完 onboarding → 看完整画像 → **没有钩子把他推到注册**。
- **决策（用户 2026-09-14 拍板 4 点）**：
  - 首页：一句话钩子 + 主 CTA（看测试 / 看画像，按访客画像状态分流） + 副 CTA（"登录 / 注册"） + 既有 privacy / locale picker
  - 画像 partial：访客看 ① 你说过的 / ② 金钱底色 / ③ 3 个瞬间（自己说话的素材）；AI 推断的 ④ 我听到的可能 / ⑤ 给未来的你 **上锁**（这是最有钩子价值的两段）
  - 非测试页：全部 hard redirect 到 `/login?next={path}`（访客挡得只剩 `/`、`/onboarding`、`/portrait` 部分、`/login`、`/recover`、`/privacy`）
  - API 端：所有被访客挡的页面对应 API 加 `401 {error:'unauthorized', reason:'signed_in_required'}`（**防 curl 绕过页面的 UX redirect**）
- **修复（helper）**：`src/lib/identity.ts` 加 `isSignedIn(identity)` 纯函数 + `requireSignedIn(locale, nextPath)` 页面闸（hard redirect，路径写死不走 request.url）；**新增 `src/lib/api-auth.ts`** 含 `requireApiUser(): Promise<ApiAuthResult>` discriminated union。
- **修复（落地页 4 状态分支）**：`src/app/[locale]/page.tsx` 重写——已登录有画像 → journeyHrefFor()；已登录无画像 → /onboarding；访客有画像 → ctaPortrait + 副 CTA ctaSignIn；访客无画像 → ctaTest + 副 CTA ctaSignIn。
- **修复（画像 partial）**：`PortraitSections` 加 `preview?: boolean` + `lockedHref` props；preview=true 时 ④⑤ 替换为 `<PortraitLockedCard>`；校准入口 + 版本对比 + 一路走来的画像 三处也隐藏。**新增 `src/components/PortraitLockedCard.tsx`**（纯文字块 + 居中 CTA，无模糊效果——访客需要看清"这是被锁的内容"）。/portrait 顶部 `<p data-guest-banner>` 引导条。
- **修复（11 个非测试页 hard redirect）**：`/chat` `/journal` `/letters` `/road` `/timeline` `/journey`（身份闸在 uiVersion guard 之前）`/journey-new`（同上） `/journey/changes` `/vip` `/me` `/archive` —— 首行加 `await requireSignedIn(locale, /${locale}/{path})`。
- **修复（简化附属组件）**：`VipView` 删 `loggedIn` prop + 删除三态 guest 分支（硬闸后永远登录）；`MeAccount` 不动（已 always render）；archive 页去掉 `isSignedIn` 计算 + SignOutButton 包裹条件（硬闸后永远渲染）；`/me` 去掉 guest note + TouchOptIn 包裹条件。
- **修复（13 个 gated API 加 401）**：`/api/chat/sessions` `/api/chat/[sessionId]`（POST + DELETE） `/api/journal`（POST + GET） `/api/journal/[id]/reply`（**401 在 VIP 403 前**） `/api/letters`（POST + PATCH） `/api/portrait/evolve` `/api/journey/{assess,changes,anchor,experiment,close}`。`/api/onboarding/*` `/api/companion/nudge` `/api/quota/*` `/api/ui-version` `/api/auth/*` **保持开放**（测试本身 + 无用户数据 + Better Auth 自管）。
- **i18n 四语同构**：`landing.hook/hookSub/ctaTest/ctaPortrait/ctaSignIn`（5 键）；`portrait.guestBanner`（1 键）+ `portrait.locked.{script,toFuture}.{eyebrow,title,body}`（6 键）+ `portrait.locked.cta`（1 键）。**英文/日文/繁体保持自然口语（不是直译）**。
- **data-* 锚点**：`[data-landing-hook]` `[data-landing-cta-test]` `[data-landing-cta-signin]` `[data-portrait-locked]` `[data-portrait-locked-cta]` `[data-portrait-section="spoken|baseColor|moments|script|toFuture"]` `[data-guest-banner]` —— verify-stage 走锚点不走 innerText（RSC flight 假阳性/假阴性，smoke-m9 注释已踩）。
- **新测试**：`src/tests/api-auth.test.ts`（4 用例：契约 + union shape ok:false response 形状）+ `src/tests/identity.test.ts`（4 用例：isSignedIn 三态）+ `src/tests/portrait-partial.test.ts`（21 用例：4 语 × 2 section 锁卡 + 4 语 guestBanner + 4 语 landing 5 键）。
- **verify-stage 新增 §⑪ 访客门禁 4 段断言**：`landingGuestHookTest`（2 语 × 钩子 + 主 CTA + 登录 CTA + href）/ `portraitPartialTest`（empty 分支 0 锁卡 0 banner）/ `guestRedirectPagesTest`（11 个非测试页 → /login?next=）/ `apiUnauthorizedTest`（9 个 POST + DELETE + reply 全 401）。
- **验证**：tsc 0 错 + **365 单测全过**（含 +24 新用例）+ verify-stage 全面覆盖（含新 §⑪）。**访客 capability matrix 偏离 `docs/02 §7`** 已登记 `docs/08-待修清单.md` 附E-6。

### 2026-09-15 · bugfix 金钱关系测试 401 阻塞：onboarding_talk 对访客放行

- **触发**：用户 2026-09-15 反馈"看看你和钱的关系，点进去怎么是聊天，不是金钱关系测试"。
- **根因**：上一批"访客分级"（本表上一条，2026-09-15）把 `/api/chat/sessions` 和 `/api/chat/[sessionId]` 整体加 401（`src/lib/api-auth.ts` `requireApiUser()`），但 chat 是 **kind=onboarding_talk（M3 体检初谈，对访客开放）+ kind=chat（M4 正式对话，需登录）共用的同一条 API**——闸门太粗误伤 onboarding_talk。
- **影响**：访客进入 `/onboarding` 欢迎屏 → 点"开始" → `/api/onboarding/talk` 起 session 成功 → **第一条用户消息发出去就 401**（`/api/chat/${sessionId}` POST 挡）。UI 死在对第一句 AI 开场，用户以为是"被扔进了聊天页"。
- **修复**：把 `/api/chat/[sessionId]` 的 401 改为"**先 resolveIdentity + getSession，再按 session.kind 分流**"——kind=chat 保持 401（正式对话仍要求登录），kind=onboarding_talk 对访客放行（金钱关系测试可走完）。POST 与 DELETE 同口径。`/api/chat/sessions` **不动**（只创建 kind=chat，必须登录）。
- **实现**：闸门判定抽出为纯函数 **`src/lib/chat-auth.ts` `gateSessionChat({ session, identity })`**（返回 `SessionGateResult` discriminated union）——POST/DELETE 各自调一次；这样 route handler 内部就是 5 行，单测也无需起 NextRequest runtime。
- **新测试**：`src/tests/chat-auth.test.ts`（5 用例：2 × 2 kind×身份 全组合 + 1 boundary 字段不被改）。
- **验证**：tsc 0 错 + **370 单测全过**（含 +5 新用例）+ `docs/08-待修清单.md` 附E-6 增补。

**问题**：本文的**结构性章节停在 M9** —— M7 / M11 的变更只写进了修订记录，**没有回写到 `§2 目录结构` / `§3 数据库表` / `§5 上下文组装` / `§6 安全层` / `§8 i18n` / `§9 环境变量`**。照旧版写码会踩实打实的坑。

**已修（逐节）**：

- **§2 目录结构（7）**：`content/` 补"书"这一层（`content/books/<bookId>/<locale>/`）；`middleware.ts` → **`proxy.ts`**（Next 16 改名）；补 `deploy/`；页面 8→19、API 11→31、lib 10→37、scripts 1→18。
- **§3 数据库表（8）——最严重**：补 `growth_profiles.daily_seen`、`chat_sessions.safety_flagged`、**M11 三列 `books`/`threads`/`touch`**、`portrait_versions.bookId`（+ `source` 枚举加 `book_switch`）、六个数组的 `bookId?`；并补**三张整表**：**`consent_records`**（含 `kind='sensitive'|'touch'`）、`events`、`recovery_attempts`。
  ⚠️ 原 §3 **没有 `consent_records`** —— 照它建表，合规清单（docs/02 §9）里"敏感信息单独同意"这个 MVP 门槛**没有落地处**。
- **§5 上下文组装（3）**：路径改 `content/books/{bookId}/{locale}/journey/`；practices 加 `bookId` 过滤；**补第 6 层「命题线 `threads`」**（多书交叉印证的唯一数据源 —— 代码早已注入，**文档不写等于不存在**）；补 `LANGUAGE_RULES`（优先级仅次于安全规则）。
- **§6 安全层（2）**：覆盖入口 **4 → 5**，补 **`journey/experiment`** —— M11-D 的微行动与「合」的"实际发生了什么 + 一句感受"走的就是它；词表路径改 `content/safety/keywords/{locale}.json`。
- **§8 i18n（3）**：`middleware.ts` → `proxy.ts`；内容路径改 books 层；**`scripts/check-content.mjs` 该脚本不存在**（核实）→ 实际是 `build-content.mjs` + `pnpm check-content`。
- **§9 环境变量（4）**：`NEXT_PUBLIC_SITE_URL` → **`NEXT_PUBLIC_BASE_URL`**（代码实际读的）；补 `TOUCH_CRON_SECRET`（**没配 → `/api/touch/dispatch` 直接 503**）、`TRUSTED_ORIGINS`、`SUPPORT_EMAIL`、`APP_NAME`；改成分组表格。

**连带缺口（已进 `docs/08-待修清单.md` P1-2）**：`.env.example` 与代码读取的变量对不上，缺 `TOUCH_CRON_SECRET` / `NEXT_PUBLIC_BASE_URL` / `TRUSTED_ORIGINS`。

**纪律（新增）**：**表结构的唯一真源是 `docs/05 §4` + 代码**；`§3` 是速查副本，**改表必须同步本节**。

### 2026-09-14 · M11 设计定稿：多书架构 + 触达层（产品口径见 docs/02 §12，新文档 docs/05）

用户 2026-09-14 定案，新增 **docs/05-多书架构与触达层.md**。要点：

- **认知成长基于「用户」，不基于「书」** —— 三层分离：成长档案归用户 / 内容层归书 / **命题线是桥**。
  桥已架好一半：`src/lib/content.ts` 的六命题 `TOPICS` + `TOPICS_ZH` 已存在，内容 frontmatter 也已打 `topics:` 标签，**但只挂在内容上、没挂到用户身上**。
- **书 = 地图上的「区域」，不是「进度条」**；**灯必须从书的阶段里拆出来**：书的阶段＝该书路线图（可换），用户的灯＝**命题上的程度**（不可重置、永久累加）。
  实现 = `src/lib/stage.ts` 的 `STAGE_LAMPS` 索引加 `bookId` + `LampRule` 加 `topic`，**不改表** —— 旧书的灯 `kind` 不变，因此 `confirmed.lamps` 历史**一个字不动**；评估确认时增加一个副作用：按 `topic` 派生证据写入 `threads`。
- **数据模型：零新表。** 全部走 `growth_profiles` 行内 JSONB（加 `books` / `threads` / `touch` 三列；已有数组 `memories`/`experiments`/`letters`/`stamps`/`dailySeen`/`concerns` 加 `bookId?`；`portrait_versions` 加 `bookId`，`UNIQUE (user_key, version)` 不变）。
  理由：§3 自己写明了这是**故意**的设计 ——「在行内 → **导出/删除/游客迁移天然覆盖**」。新建表则必须手工补三处，**必然漏一处**：`src/lib/migrate.ts` 迁移白名单 + `src/app/api/me/export/route.ts` + `src/app/api/me/delete/route.ts`。
- **6 个阶段验证节点零 DDL**：第 N 天 = `now - growth_profiles.created_at` **纯函数推导**（与既有「读时纯函数推导，自愈」纪律一致）；结论落**已有** `events` 表（`name='node_verdict'`，`metadata={node, verdict, metric, value}`）。
- **优化留痕不进库**：它是**产品变更日志**，天然该跟代码走版本控制（仓库内 `optimizations.json`，读时作为 `contentVersion` 注入）。进库反而要新建表，破坏"零新表"。
- **内容层**：`content/<locale>/journey/` → `content/books/<bookId>/<locale>/journey/`（`safety/` 不分书、不动；`practices/` 保持中文单一事实源，加 `bookId`）。连带改 `scripts/build-content.mjs` 与 `src/lib/content.ts` 的 `ContentBundle`。
- **触达层**：一期 H5 + 邮件 → 二期 iOS 通知（人群走海外路线）。**复用已在运行的 `src/lib/email-resend.ts`，不新建通道**；召回按产品的原生"信"形态写。
  ⚠️ **v1 目前没有任何定时能力**（只有 `scripts/*.mjs` 手动脚本）→ **调度是本次唯一全新的系统能力**（自托管先用 `systemd timer`）。
- **上手路径**：把「我听到的是…」提前到第 2 分钟（第一个物件）；**顺序铁律：先被说中 → 再注册/同意/定价** —— P§9 的敏感信息单独同意弹窗要从"画像生成前"挪到"被说中之后"。
- **日频形态**：`/journey`「三件事」→「**一段**」（开/做/合 + 明确收束）。
- **⚠️ 唯一一处改动既有表：`consent_records` 加 `kind` 列**（已实读确认，非猜测）。
  现状表结构 `(id, user_key, granted, policy_version, ip_hash, created_at)`，**没有类型字段** —— 它是专门为「敏感信息单独同意」建的（`src/lib/consent.ts` docstring）。触达 opt-in 必须与它**分开存、分开读**，否则「同意生成画像」与「同意收邮件」混成一条。
  改法（**已实现于 `87d53da`**）：`ALTER TABLE consent_records ADD COLUMN IF NOT EXISTS kind TEXT NOT NULL DEFAULT 'sensitive'`，`ConsentKind = 'sensitive' | 'touch'`；新增索引 `idx_consent_user_kind (user_key, kind, created_at DESC)`，旧索引 `idx_consent_user` 冗余保留。
  **默认值方向必须保守**：默认 `'sensitive'` → 漏传 kind 只会退回改动前的旧行为；**绝不能默认 `'touch'`**（漏传就直接违规发信）。
  **⚠️ 连带已修一个静默坑**：`latestConsent(userKey)` 原来**没有按 kind 过滤**，加列后会跨类型取最近一条（用户刚点"同意收邮件"→ 画像入口误判成"已同意敏感信息"）。实现已改成 `latestConsent(userKey, kind = 'sensitive')`（WHERE 加 `AND kind = ${kind}`）。**它不报错、只静默出错，所以验收必须有"两种同意互不串台"的单测。**
- **待核 4 项**见 docs/05 §11（第 1 项已结）。
- **验收报告：`docs/07-M11验收报告与优化建议.md`** —— 产品验收侧。含 P0 认知层（每日两问 / L4·L5·M / 第③问 / 6 节点未实现）、P1 触达层 8 条、P2 多书未接线 3 条、**§6 线上实测 12 条（L1–L12）**。**动手前先扫这份的未决项。**

### 2026-09-13 · 进度栏全阶段可见 + 行动按钮=真实记录（产品口径见 docs/02 §12）

- **JourneyProgressBar 重写**：单行左标签+右计数 → 四列布局（`data-stage-cell`），每列 = 段名行 + 进度行（`data-segment` 条 + 计数 span）。计数只在当前段带 `data-lamp-count` 属性（smoke 唯一锚点）；`{`${lit}/${total}`}` 必须是单表达式——JSX 里 `{lit}/{total}` 会被 RSC 注释标记切开，`.includes('3/3')` 断言假阴（踩过）。stageLampScales 语义不变（走过段整段填充）。
- **RecordForm 新组件**（client）：实际发生了什么（必填，maxLength 300=API ACTION_MAX）+ 一句感受（可空 500）→ POST /api/journey/experiment——复用微行动全管线（checkSafety/appendExperiment/bumpActiveDay/LLM 收条），零新端点零新表。saved 停在现场显示收条。
- **ActionRecordList 新组件**（client）：评估 actions 每条一个按钮 → 就地展开 RecordForm（anchor 显示这条行动是什么）；StageLampChart 与 journey 页阶段 4 块共用。灯行入口（lampForm state）直接用 RecordForm。原先 Link→/chat 的行动按钮全部替换——按钮的去处是记录，不是聊天。
- **i18n**：+3 键（recordWhatLabel/recordWhatPlaceholder/recordAnchor）+ lampActCta 改语义，四语同构；复用 microFeelingPlaceholder/microSave/microSaved/chat.safetyNote。
- **smoke-m9**：+2（四段格子+段名全可见；各段计数与 data-lamp-count 唯一锚点）、灯图 CTA 断言改「真实记录入口（不跳通用聊天）」。tsc + 159 单测 + m9/m10 全绿。

### 2026-09-13 · 以图为中心：StageLampChart 雷达灯图 + 行动按钮化（产品口径见 docs/02 §12）

- **几何纯函数** `stage.ts` `lampChartPoints(n)`：单位圆顶点均布（顶部起始顺时针；3=三角、2=上下两点、0=[]），渲染层乘视窗尺寸；单测断言圆上距离与 120° 角距。
- **新 client 组件** `StageLampChart`：SVG（viewBox 220×200，圆心 110/100 半径 64）——虚线参考圈（标准）+ 点亮顶点 polygon（fillOpacity 0.12，≥2 盏亮才连形）+ 节点（亮=实心 accent+光晕、未亮=空心 paper+描边）；SVG 节点与图下灯行共用一个 `open` state（默认展开第一盏未亮）。灯行展开：未亮=hint+`data-lamp-cta` Link（chat），已亮=evidenceLead+依据。行动按钮组 `data-action-cta={i}` 每条 Link → chat。
- **journey 页接线**：current 段文字灯列表整体替换为图（evidence 以 `Object.fromEntries(confirmedEvidence)` 传对象——Map 不可跨 RSC 边界）；actions 来自 `confirmed?.actions ?? []`。阶段 4（checks.length===0）不画图，行动按钮在页内单独渲染同构块；报告块删除文字版 actions 列表（按钮已承担，避免同屏重复）。
- **i18n**：`journey.lampChartCaption`（图注：点亮范围=现在的位置/虚线外圈=标准）、`journey.lampActCta`（「对它说说这件事」）四语；计数复用 `progress.lampCount`。
- **smoke-m9 +4 断言**（advance 后 barHtml）：3 节点全 `data-lit="0"` 且无 polygon（0 亮不连形）、caption+灯行锚点、未亮默认展开（hint+cta）、2 个 action 按钮（ACTION_SEED 在场）。tsc + 159 单测 + m9/m10 全绿。

### 2026-09-13 · 走过的段也展示灯 + 报告阶段标注（产品口径见 docs/02 §12）

真实推进第一次发生后暴露的呈现缺口：journey 页 stages.map 只展开当前段（`current &&` 守卫），推进后走过段的灯/依据/心印/报告全部消失。修订（journey/page.tsx 单文件 + i18n）：

- **stProgress 按段现算**：`computeStageProgress(s.id, profile)` 下沉进 map（原顶层只算当前段）；顶层 `progress` 变量删除。
- **走过段灯块**（`walked && stProgress.checks.length > 0`）：计数恒 `n/n`、三盏灯全 ●（走过即整段点亮——推进即评估结论，呈现层灯不灭成立）；依据走 `confirmedEvidence.get(kind)`（最近一次确认评估还覆盖该段时显示），不渲染 hint 与 waiting 行。
- **走过段心印块**：条件 `current` → `current || walked`（stageStamps 已按段前缀过滤 + 渲染侧 kind 去重兜底）。
- **报告阶段标注**：报告块标题下新增 `assess.latestStageNote.replace('{n}', actualStage)`——单表达式单文本节点，SSR 无 `<!-- -->` 切分，smoke 可直接 `.includes` 断言。报告真值仍是 `confirmed`（最近一次），标注只声明它属于哪次评估。
- **smoke-m9 +5 断言**（advance 到 stage 2 后的 barHtml）：走过段 3×●（含评估未亮的 color 灯）、EVIDENCE_A/B 随灯、hint 收起（无 "The bar: "）、走过段心印、`Last assessment · Stage 2` 标注。tsc + 157 单测 + m9/m10 全绿。

### 2026-09-13 · 阶段开启联动：灯全亮 ⇒ 下一阶段自动开启（产品口径见 docs/02 §12）

用户指正「评估已完成阶段 1 就该自动开启阶段 2，不该硬性要求用户完成动作；除非评估未完成需要补充信息」。素材闸（≥3 条或 14 天）从此只管「再评一次的时机」，不再挡「阶段开启」。四处落地 + 一处存量自愈：

- **validateAssessment 硬约束**：`rules.length > 0 && rules.every(lit)` ⇒ `actualStage === Math.min(stage+1, MAX_STAGE)`，否则判无效草稿（重试一次后仍失败走 502 fail-safe）。反向不强制（有灯未亮仍可判 stage+1，评估自由在门口）。
- **buildAssessMessages**：actualStage 说明改写——「本阶段全部灯点亮时必须写 stage+1（程度已达成，下一阶段随之开启，不需要更多素材证明）；有灯未点亮时写 stage」。
- **路由 confirm 收口**：confirm 后若 `pending.actualStage > profile.stage` → 随确认自动推进（appendStamps stage{n}_entered + saveStage + stage_advanced 埋点），防旧缓存 UI 造出「全亮但停在原地」。
- **getProfile 加载自愈（含存量迁移）**：读档后调纯函数 `stageAdvanceTarget(profile)`（stage.ts：确认评估本阶段灯全亮 → stage+1，否则 null）——非 null 即执行推进三件套（心印/存档/埋点，镜像 advance）。幂等：推进后灯集属新阶段条件不成立；并发同写同值无害（appendStamps DISTINCT ON 去重）。存量「全亮停阶段 1」账号（含真实用户）下次访问自动进阶段 2，无需手工 SQL。
- **AssessCard**：canAdvance（评估判在下一阶段）时不再渲染「留在阶段 n」（stayCta 键保留字典但停用）——不同意的出口只剩「不是这样的」。
- **测试**：validate 全亮矩阵（stage1/3 全亮停原地拒、全亮进下一阶段过、部分亮不受限）+ stageAdvanceTarget 矩阵（全亮推进/部分亮 null/stage4 null/只认本阶段灯集——推进后旧灯全亮不连环跳）。157 单测 + smoke-m9/m10 全绿（m9 种子全部部分点亮、m10 断言不依赖 profile.stage，均无碰撞）。

### 2026-09-13 · 评估报告人称钉死：一律第二人称「你」（用户指正）

用户在旅程页看到点亮依据「它记得：**他**自然说出了……」——报告是念给用户听的，却用第三人称描述他。根因：buildAssessMessages 的 JSON shape 里 summary/diagnosis/distance 三条钉了「第二人称」，但 evidence/actions/nextHint 三条漏钉，而 system 脚手架是见证者视角通篇用「他」，LLM 写漏钉字段时顺着脚手架人称漂移。修复：三个漏钉字段补「第二人称/用『你』」；红线区新增总则「所有给用户看的文字一律用第二人称『你』直接对他说话——这份报告是念给他听的，不是向第三方汇报他；system 里用『他』指代只是内部视角，不得带进任何输出字段」。单测新增人称断言（153 全绿）。已确认的存量报告按只增不减保留，下次评估确认后自然换新。

### 2026-09-13 · 程度制标准：标尺内容从动作句式改为程度句式（产品口径见 docs/02 §4/§12）

用户指正「评估的标准只是用户做了什么、做到了什么程度，希望的标准是离活法在本阶段应达到的程度」。灯判定真值维持上一条修订（confirmed.lamps），本条只换标尺内容——**零代码逻辑变更，纯文案/prompt 同步**：

- **`src/lib/stage.ts` STAGE_LAMPS**：8 条 hint 全部重写为程度句式（kind/labelKey/结构不变），文件头与 doc 注释同步（「每盏灯是本阶段离活法应达程度的一个切面——判定的是状态到达了没有，不是做过哪些动作」）。hint 直接进评估 prompt 与 journey 页展示，两处自动同源。
- **`src/lib/assess.ts`**：灯清单引言改为「判定的是他到达了这个状态没有，不是他做过哪些动作」；红线组新增「判定程度，不数动作：……不要核对任务清单、不要数次数」（置于「证据必须来自素材」之后）。
- **i18n 四语**：journey 段 8 个 label 键（stageLampStory…stageLampReview）改程度短语、8 个 hint 键（…Hint）改程度句式；stageLampHintLead/stageLampWaiting 等其余键不动。
- **content**：`content/{zh-CN,en}/journey/{1,2,3}-*.md` 的 advance_when 共 8 条改程度句式（喂评估 LLM 作 stage 标尺）；`pnpm build-content` 重新生成 content.json。阶段 4 文档无灯不动。
- **测试影响面核实**：content.test.ts 只断言 advance_when 非空；assess.test.ts:289 只断言 hint 字符串在 system 中（toContain 非字面）；stage.test.ts 只断言 hint 非空——文案重写零破坏。vitest 152 全绿；smoke-m9/m10 全绿（冒烟断言用 data-* 属性与种子文本，无文案字面依赖）。

### 2026-09-12 · 灯判定真值二次修订：灯 = 评估结论，不是动作档案（产品口径见 docs/02 §4）

用户指出的语义错误（原话要点）：「灯不要代表用户的交互动作，要呈现评估标准评估后的当前进度」。此前展示层 done = stamp 存在（确认仪式入档的印），沿用了机械时代「印=点亮」的直觉——存量印/动作印会让灯亮起来，展示与评估结论脱钩。修订：

- **`computeStageProgress`**：done = 最近一次确认评估 `stage_assessment.confirmed.lamps` 里这盏灯 lit（`verdict.get(kind) === true`）；评估没说亮的灯一律不亮，stamps（含存量机械印）完全不参与判定。`StageCheck.kind` 语义改为「关联评估结论与确认时入档的 kind」，展示名统一走 `dict.journey.<labelKey>`（不再用心印见证文案 dict.stamps——那是动作陈列的语言）。
- **`stageLampScales`**：走过的段整段填充（阶段推进只发生在评估确认仪式里，「走过」即评估结论）；当前段按最近一次确认评估点亮的灯数；未到的段全空。边界：推进后的确认评估还是上一阶段的灯 → 当前段从 0/3 起（诚实：新阶段的灯还没评过）。
- **journey 灯列表**：点亮/未点亮统一渲染 `●/○ + 灯名`，点亮灯下追加评估依据（confirmed 的 evidence），每盏灯下展示点亮标准（labelKey+'Hint' 四语，与评估 LLM 同一张 STAGE_LAMPS 判定表——评估不再黑盒）；「灯全亮≠终点」补丁文案不采纳（用户否决：呈现本身就该是评估结果，不需要解释性文案）。
- **AssessCard review 卡**：点亮灯标签同步改灯名（原 dict.stamps 动作文案）。
- **stamps 角色重述**：确认仪式同步留下的心印存档（足迹/心印陈列用），仍只增不减、仍由 appendStamps 原子幂等写入（自愈历史重复），但不再是灯的判定来源。
- **测试**：stage.test.ts 重写判定矩阵（评估结论真值 / 存量印不多算 / 无评估全不亮 / 走过段满+当前段 0 起）；smoke-m9 进度条断言改 seg1 width:100%（走过段整段填充）。vitest 152 全绿；smoke-m9 + smoke-m10 全绿。i18n 四语同构追加 8 灯标准键 + stageLampHintLead。

### 2026-09-12 · M10：机制验证修订（已实施，产品口径见 docs/02 §5/§10/§11/§12）

消化外部机制评审，四项拍板（产品内验证 / 维持受众 / 被看见但不追问 / 轻量承接）。**零 schema 变更**——全部落在既有 JSONB/experiments 结构内；验证仪表漏斗仍由 events 派生聚合，掉队点地图仍为离线查询（隐私备注不变：只做聚合分布，不做个人画像输出、不触发面向用户的差异文案）。落地明细：

- **微行动即时见证回应**：`src/lib/receipt.ts`——`buildReceiptSystem` 收条体 prompt（确认收到/承接感受；不分析不解读行为含义、不给建议、结尾不提问、不复述动作细节、不说「真棒/继续加油」式加压；长度语言感知：zh 15-60 字 / en 10-40 words；语言钉死行）+ `buildReceiptUser` + `RECEIPT_FALLBACKS`（四语各 3 条，`pickReceiptFallback` djb2 确定性抽取，未知语言回落 en）。`POST /api/journey/experiment` 写入 experiments 后调 LLM（maxTokens 200 / temp 0.7；安全命中直接给转介文案不调 LLM；失败静默降级词典收条，绝不阻塞记录）；响应加 `receipt` 字段，MicroActionCard saved 态展示。埋点：首次完成 `micro_action_first_done`（恰一次）、每次展示 `micro_action_instant_response_shown{llm}`。
- **《我变了什么》变化清单**：confirm/advance 把旧 confirmed 移入 `stage_assessment.previousConfirmed`（首评形态为 null）；narration 缓存 `changeList{text,generatedAt,forConfirmedAt}`（`forConfirmedAt===confirmedAt` 才命中，新确认即过期）与自过期锁 `changeListLockAt`（3 分钟，镜像 claimAssessment）同放该 JSONB。`src/lib/changes.ts`：`buildChangeListView` 纯函数（位置 A→B / 新灯差集 = lit+有依据且上次未点亮（首评全算新）/ actions 直出——结构化部分不依赖 LLM，叙述段只是叠加层）+ `buildChangeListMessages`（红线：素材只来自入库内容禁编造 / 不评判不打分不比较 / 没变化就说没变化——诚实也是陪伴 / 无理财建议 / 长度语言感知 zh 150-400 字 en 100-250 words；prev 有无决定「上次→这次」与「第一份清单」两节互斥）+ `gatherChangeListMaterial`（baseline=confirmedAt）+ `claimChangeList`/`releaseChangeListLock`/`saveChangeList`。新路由 `POST /api/journey/changes`（缓存命中幂等返回；抢锁防双击双生成；validate 失败清锁 502 fail-safe；maxDuration 120，全免费）；新页 `/[locale]/journey/changes`（入口在 journey 确认报告块尾部）；埋点 `change_list_generated{cached}` / `change_list_viewed{narration}`。
- **0 元替代版 + 命题标签（content 层）**：journey frontmatter 可选 `free_alt`（en 必须双引号 YAML 标量，防冒号+内嵌引号解析坑）+ `skippable: true`（阶段 1 微行动注记「今天不想做也没关系」）+ `topics`（六枚举 self-worth/parents/inner-turmoil/boundaries/money-safety/allowing，中文标签 `TOPICS_ZH` 在 content.ts，build-content.mjs 硬编码副本须同步）；build-content 校验器三段（free_alt 非空 / skippable 布尔 / topics 非空数组且 ⊆ 枚举）；MicroActionCard idle 态在输入框上方渲染 free_alt。
- **命题标签进评估 prompt**：`buildAssessMessages` system 在阶段有 topics 时加「本阶段涉及的命题：…（diagnosis 归属参考，不必点名）」，无则整行不出现。
- **归因前后测（借阶段评估对比承担）**：评估 system 加「归因对照」行——prev 在场时提示可自然描述他两次面对同类场景的说法变化（如从「我不行/都怪别人」移向「我当时真正想要的是什么」），只描述看见的变化、不打分不比较；首评（无 previousConfirmed）不出现该行。v1.5 周期回顾再加正式重述题。
- **测试**：`receipt.test.ts` / `changes.test.ts` 新建（红线逐字、长度语言感知、fallback 确定性与词典边界、新灯差集、narration 缓存过期、prompt 两节互斥、validate 30..3000）；`content.test.ts` 加 topics⊆枚举 / free_alt 非空 / skippable 布尔 / 阶段 1 skippable=true / 阶段 2 free_alt 非空；`assess.test.ts` 加归因对照行与命题行有无用例。单测 151 全绿（15 文件）。`scripts/smoke-m10.mjs`：journey 页 stage2 free_alt 在场 + stage3 不串场 + changes 入口；微行动 receipt 非空 + 三事件恰一次；changes 真生成（cached:false + forConfirmedAt 对齐）或 502 fail-safe 锁清、新灯差集渲染、幂等 cached:true 不重烧、viewed 埋点；confirm → previousConfirmed 逐项承接 + confirmedAt 前移 + 旧缓存过期；导出含 previousConfirmed/changeList；级联删除归零（含 events）。

### 2026-09-12 · M9 二次修订：诊断式评估报告 + 首评前置 + 心印刻度进度条

用户三轮递进（进度值缺失 → 积分/等级味警惕 → 「要直接的诊断和下一步行动建议」）。产品口径见 docs/02 §4「阶段进度评估标准」/§12。技术落地：

- **`StageAssessment` 扩展**（`profile.ts`）：新增 `diagnosis: string`（为什么是这里——机制框架引原话）、`distance: string`（离活法多远——路标式）、`actions: string[]`（2-3 条具体小步）。存储位置不变（`growth_profiles.stage_assessment` JSONB 行内），导出路由枚举整对象自动带上，级联零改动。
- **`validateAssessment` 新增结构边界**：diagnosis 10..800、distance 10..600、actions 数组 1..4 条且每条 trim 后 ≤200（空白条目过滤后数量不一致 → 拒绝半成品）；风格长度交 prompt。
- **`PortraitEvidence` 进评估素材**（`AssessMaterial.portrait: PortraitEvidence | null`）：`questionnaire.script_source`(300)/`concerns_seed`(200)/`spoken[]`(各 160)/`moments[].detail`(160)/`baseColor`(240)/`script`(240)/`scriptStatus`/`confirmedSections`（calibrations verdict='hit' 的 section）。**不参与 baseline 过滤**（画像是当前态自述）；choice 类问卷选项码与 `toFuture` 不进（无语义/是愿望，且会诱导违规点灯）；`scriptStatus` 必须随 script 陈述（pending=候选不得当认可、rejected=否认也是态度、confirmed=认可）。全部字段入库时已过 checkSafety，不再重复过安全层（与 chat prompt 同级别同字段域，无新暴露面）。
- **首评闸**（`shouldOfferAssess`）：锁/冷却/pending-TTL 三挡板之后、素材闸之前插 `if (!state.confirmedAt) return true;`——刻意只带这一个条件：冷却过期仍会再提（否则 dismiss 一次的新用户永远锁死在无进度态），pending 超 TTL 的遗留物落到这里可被 generate 覆盖重评。闭环：confirm/advance 写 confirmedAt → 闸关闭 → 回素材闸（baseline=confirmedAt）。无画像与 stage≥4 短路仍在最前；journey 页与 API 共用同一函数，零分叉。
- **prompt 诊断式改造**（`buildAssessMessages`）：system 定义报告四问（他在哪 actualStage+lamps / 为什么是这里 diagnosis / 离活法多远 distance / 下一步做什么 actions）+ JSON shape 三行 + 红线更新（diagnosis 只解释心理机制不做医疗诊断不用病症词汇不评判人格；distance 不给数字承诺不制造焦虑；独立一行「不出现任何理财建议」；体检画像也是素材但须与近期言行互证；宁可点得少不可编造）。user 消息新增置顶节「## 体检画像（他说过的关于自己的话）」（`portraitBlock` + `scriptStatusNote` 三态陈述，空子项过滤，portrait:null → `(无)` 不崩）。**长度规则语言感知扩展**：diagnosis en `60-120 words`/zh `80-200 字`；distance `30-80 words`/`40-120 字`；action `20 words or fewer`/`30 字以内`（沿用 summary 教训：en 按词 zh 按字）。route 两处 `maxTokens` 1500→2600（长报告防截断）。
- **`stageLampScales`**（`stage.ts` 纯函数）：`[{id, lit, total}]`×4，按 STAGE_LAMPS 统计 stamps 里的灯 kind（`stage{n}_entered` 等非灯印天然被过滤，重复 kind 用 Set 天然去重）；stage4 total=0。**`JourneyProgressBar` 重写为心印刻度**：各段按实际点亮比例填充（当前段 `bg-accent`、已过段 `bg-accent/50`、未至 `bg-line`；阶段 4 当前=整段 `bg-accent/40` 无刻度无右值），右值「已点亮 x/y 盏」仅 total>0 渲染；无百分比无「还差多少」。数据锚点：段容器 `data-segment={id}`、右值 `data-lamp-count="lit/total"`、AssessCard offer 容器 `data-assess-offer`（RSC flight 词典假阳性的规避手段）。
- **journey 页常驻报告块**：灯列表之后渲染 `assessment.confirmed`（summary+diagnosis+distance+actions，标题「它看见的你」）——确认过的报告不消失。
- **测试**：`assess.test.ts` 重写——素材闸矩阵改挂已评过 fixture（`confirmedAt: daysAgo(30)`，否则新闸下恒 true 语义错）；新增首评矩阵 8 例（全空 state+零素材→true / dismissed 1 天 false·14 天 true / 锁在途 false / pending 未超 TTL false·超 TTL true / confirmedAt 非空+零素材+90 天 false / portrait:null、stage:4 false）；validate 三件套越界矩阵；buildAssessMessages 断言四问/红线逐字/三态陈述/语言感知长度/画像节字段。`stage.test.ts` 加 `stageLampScales` 用例（混入 `stage4_entered` 与重复 kind、stage4 total=0）。smoke-m9：⑤ 注入 pending 带三件套种子（review 渲染+confirm 落库+常驻报告块）；新增进度条断言（seg1 `width:67%`=Math.round(2/3)、seg2 0%、`data-lamp-count="0/3"`）；**⑤b 首评前置子场景**（独立号种子画像+全空素材 → `data-assess-offer`+proposedSeenAt 落库 → generate 非 403 → 注入确定性 pending confirm → confirmedAt 落库+卡消失）；⑧ 级联清理首评号。单测 133 全绿。
- **隐私备注**：画像内容进评估 prompt 与画像进 chat prompt 是同级别同字段域的既有暴露面，无新增第三方传递；评估素材同样只进 LLM 上下文，不入任何日志。

### 2026-09-12 · M9

- 新增 `src/lib/stage.ts`（推进判定表 + `computeStageProgress`/`pendingStamps`）、`src/lib/evolution.ts`（portrait_versions 访问 + 基线/素材/提议纯函数 + 演进 prompt）、`src/lib/timeline.ts`（六源归并）；新路由 `POST /api/journey/advance`、`POST /api/portrait/evolve`（generate/dismiss，maxDuration=120，不消耗聊天配额）。
- **两个真库才暴露的坑（smoke-m9 抓到，已修）**：
  1. Neon HTTP 驱动把 timestamptz 解析成 JS `Date`——`String(row.x)` 得到 `"Fri Sep 11 2026 ..."` 这种无法回代 SQL、不能 `slice(0,10)` 的串。统一走 `db.ts` 新增的 `iso()`。
  2. neon sql tag 把 `${字符串}` 一律参数化：`SELECT ${SESSION_COLUMNS}` 实际执行 `SELECT $1`，返回整列垃圾行（owner 回看自己会话都 404）。列名清单必须写死字面量，已全部内联。
- smoke-m9 断言全部数据驱动（种子内容/href/DB 状态）：RSC flight payload 会把整份词典序列化进 HTML，grep 词典文案必假阳性；并发 generate 的败者对新基线返回 403 也是正确行为（真闸），不视为事故。

### 2026-09-12 · M9 修订：阶段进度从交互计数制改为认知/行为评估制

语义总纲（用户修正）：进度评估的是「用户当前表现出的认知与行为」实际所处的阶段——离核心价值「一辈子不愁钱的活法」已达成的进度——与产品内操作/交互次数没有严格关系。

- **删除**：`stage.ts` 的 `STAGE_CHECKS`/`pendingStamps`/`canAdvance`（机械计数判定）；journey 渲染补发印；`POST /api/journey/advance` 路由与 `AdvanceCard` 组件（404 验证留存）。`stage.ts` 重写为 `STAGE_LAMPS`（判定表：kind + labelKey + 中文 hint，阶段 1/2/3 各 3 盏）+ `computeStageProgress`（done = stamps 里有没有该 kind，**stamps 是点亮唯一真值**）。
- **新增** `src/lib/assess.ts`（镜像 evolution.ts 全部模式：读时现算提议闸 `shouldOfferAssess`（素材 ≥3 或 ≥14 天且有任意素材；冷却/锁/pending 30 天 TTL 各挡）+ `validateAssessment`（actualStage 限 [stage, stage+1] 整数、lamps 恰覆盖灯集、lit 必有 evidence、summary 10..800）+ `buildAssessMessages`（见证者 prompt：标尺=content 的 goal/advance_when，红线=证据禁编造/不评判不打分/无理财建议诊断）+ 原子抢锁 `claimAssessment`/`releaseAssessmentLock`/`saveAssessmentState`（JSONB `||` 合并）+ `gatherAssessMaterial`）；新路由 `POST /api/journey/assess`（action=generate/confirm/advance/dismiss；generate 先过闸后抢锁，validate 失败重试一次仍败则清锁 502 fail-safe；advance 服务端复算 `pending.actualStage > stage` 防绕；maxDuration=120，不耗聊天配额）；新组件 `AssessCard`（offer/review 双模式）；journey 页确认过的灯下展示依据（confirmed 里的 evidence）。
- **数据**：`growth_profiles` 加 `stage_assessment JSONB DEFAULT '{}'`（行内 → 导出/删除/游客迁移级联天然覆盖）；导出路由为显式字段枚举，补 `stamps` + `assessment` 两字段（评估依据属用户内容，可携权带走）。
- **LLM 长度规则必须语言感知（smoke 真库抓到的 502 根因）**：中文按字数、英文按词数——glm-5.3-flash 被语言钉死行强制英文输出时，prompt 写「80-200 字」会产出 551 字符的 summary 撞上 400 字符校验上限。修复：`buildAssessMessages` 长度规则按 locale 分叉（en 60-120 words / 80 words or fewer / 30 words or fewer），`validateAssessment` 上限 800 作为结构边界（防失控，风格长度交给 prompt）；evidence/nextHint 截断 300。测试锚点：700 字符过 / 801 拒。
- **全局进度条**：新 server 组件 `JourneyProgressBar`，挂在 layout 的 sticky 底部导航容器内（导航上方）→ 全部页面固定可见、零页面改动；layout 加 `resolveIdentity+getProfile`（try/catch 失败按 null 处理，绝不因进度条挂掉）并 `force-dynamic`。内容：左「阶段 n · 标题」（阶段 4「活法中」），右四段（已过=实心；当前=按 litCount/灯数 填充；未至=line 色；阶段 4 当前=accent/40 无刻度）；整条是包 /journey 的 Link。
- **并发语义备注**：generate 先过闸（shouldOfferAssess）后抢锁（claimAssessment），败者过闸时读到的 generatingAt 已置位 → 得 403 `assess_not_proposed` 而非 429；smoke 断言「恰一次真生成」接受 429/403 任一形态。
