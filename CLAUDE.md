@AGENTS.md

## 开工前必读（2026-09-14 起）

**⓪ 知识库镜像：`docs/kb/`** —— **设计依据都在这里，写码前该看的"为什么"全在这**。
你原本看不到 Mac 上的知识库；现在整库镜像到 `docs/kb/`（`20-1` 机制验证方案、`20-4` 需求书、`20-5` 多书思路、`20-6` 信息架构、`10-2` 认知内化机制、`40-x` 合规边界、`80-x` 验收与待修）。
- **只读**：改了会在下次同步被覆盖，**权威在知识库侧**。要改内容 → 告诉用户或 Hermes。
- **`docs/kb/README.md` 顶部有「知识库文件清单（事实基线 · 由磁盘扫描生成）」** —— 引用知识库文件前先看它；**清单里没有的名字不要引用**。
- **冲突口径**：**"要做什么"以 `docs/` 为准；"为什么、哪些方案已被否决"以 `docs/kb/` 为准**；仍拿不准问用户。
- 由 `~/.hermes/scripts/sync_kb_to_docs.py` 每 6 小时同步（`.manifest.json` 勿手改）；`docs/kb/` 在 `.gitignore` 中，属生成物。

**① 待修清单：`docs/08-待修清单.md`** —— **动手前先扫一遍**。这是**唯一的待修入口**。
含：P0 开跑前置 4 条 · P1 上线前置 7 条 · P2 接第二本书前 6 条 · P3 排期/体验 6 条 · **P4 文档与工程卫生（2026-09-14 已全部清掉，留痕与教训见 `docs/09 §10`）** ·
**附A 已闭环** · **附B ❌ 已撤销 = 不要照修** · 附C 优化建议 · **附D 怎么验（验收标准）**。

**①a P0 四条怎么做：`docs/10-P0开跑前置设计.md`** —— **动手做 P0 前先读它**。
一页一件事：**「合」压成一屏三行的一格提交**（同时采 L4/L5/M）· **每日两问协议 + 五类失败接法**（写进 `prompt.ts` 的硬约束）· **6 节点 `events.node_verdict`** · **首启重排** · **邮件真到达**。含**验收方式**与**待用户拍板的默认走向**。
⚠️ **该文署名"验收侧（Hermes）"，但并非验收侧本人撰写**（见 `docs/09 §11.3`）：**内容由验收侧复核后才算验收侧口径**，此前按"待验收侧确认"处理；有分歧写进 `docs/08`。

**①b 原始出处（要查证据时再看）**
- `docs/07-M11验收报告与优化建议.md` —— **验收侧**报告（`docs/07` 由验收侧生成）
- `docs/06 §9–§11` —— **验收侧独立核查**的原始登记与**撤销记录**（证据存档；§11 的「❌ 撤销」表列的是**不要照修**的项）
- `docs/09-技术方案一致性对照.md` —— `docs/03` ↔ 知识库 ↔ v1 现状；`docs/08` 里 P3 的详细依据

> **同一条问题在 `docs/07` 与 `docs/06` 都有时，以 `docs/08` 的合并条目为准**（已去重）。

**② 方案文档（改行为前先对齐，不要凭印象）**
- `docs/05-多书架构与触达层.md` —— 多书架构 + 触达层（M11 主文档）。**枚举名等细节以代码为准**，该文 §0.1 是实现进度表。
- `docs/06-信息架构与认知层验收.md` —— 信息架构（一级入口 4→3、页面去向、"合"压成一屏）+ 认知层指标 L4/L5/M 的采集落点 + **§9–§11 = 独立验收结论（验收人登记）**。
- `docs/03-技术方案.md` —— `§3` 表结构、`§10` 里程碑、`§11` 修订记录（**变更史**）。
- `docs/01` 定位 / `docs/02` 产品口径 / `docs/04` M8 Go-No-Go。

**③ 改了就要留痕**
- **每次改动在 `docs/03 §11 修订记录` 留一条**（产品口径同时同步 `docs/02 §12`）。
- 承重前提（人群 / 陪跑形态 / 领域焦点 / 书的可见度）被替换时，**通篇重写，不要打补丁**。
- 知识库（Obsidian）侧的同步由 Hermes 负责；**你只需保证 `docs/` 内部一致 + 留修订记录**。

**④ 三条纪律（都踩过，别再犯）**
1. **不得凭印象写引用**：文中要写"依据知识库 XX 文件 / 依据 docs/NN §X"之前，**必须核对它真实存在**。
   （2026-09-14 曾引用不存在的知识库文件 `20-6_信息架构_v4`。）
2. **不得代署他人的名**：验收结论只归验收人。
   写"Hermes 已验收 / 由 Hermes 登记"之前，**先确认真是对方写的**；自己发现的写成"待验收"。
   （2026-09-14 `docs/06` 初稿曾署"由 Hermes 补写"，实为编码侧起草。）
3. **核对要核到"写入真值的那一行"，不能停在"长得像的东西"**。
   `grep` 到一个同名类型就当真值 → 会既误删真取值、又误加假取值（2026-09-14 `safety_events.source` 被错判：真取值是 `chat｜journal｜letter｜experiment｜calibrate` 五个，`calibrate` **存在**，只漏 `experiment`；被引作依据的 `profile.ts:200` 其实是**另一张表**的 `ThreadEvidence.source`）。
   **验收侧同理**：`docs/06 §11` 的 3 条误报（坐标点击 + 长截图）也是这一族 —— **取证方式不支撑结论的强度。**

**⑤ 知识库在用户 Mac 上，服务器看不到它 —— 唯一合法读法：`docs/kb/`**
- 知识库在**用户 Mac**（`~/Documents/老爸知识库/`），**服务器上没有副本**。你**无法**直接读它、**也无法**"实测"它的文件名。
- **因此服务器侧文档不得断言知识库的文件名与内容。** 要引用时：
  ① 优先引用 **`docs/kb/`** 里实际存在的文件（`docs/kb/README.md` 顶部有磁盘扫描生成的事实清单）；
  ② `docs/kb/` 里没有的（如未镜像的 `60_讨论过程/`）→ 写「**待验收侧确认**」，不要猜。
- 2026-09-14 这个坑踩了三次（`80_验收/` 路径、`20-6_信息架构_v4`、以及一轮**双方互相**断言"对方引用虚构"）。**争议一律用可复现命令的输出裁决**（`ls docs/kb/` / `find` / `git log` 的原始输出 + **在哪台机器上跑的**），**不引用任何一方的旧文本当依据**。
- **`20-7`/`20-8`/`20-9`/`00-0` 这几个名字到底存不存在？** 知识库所在机器上的三项实测：① 当前磁盘本项目目录 **25 个文件**，无这几个名字；② 该 vault **自 2026-07-25 纳入 git 以来**，全历史（含新增/改名）`grep -E "20-[789]_|00-0_"` → **空**；③ Spotlight `mdfind` → 空。
  → **无论历史如何，现行有效名一律以 `docs/kb/README.md` 顶部"由磁盘扫描生成"的清单为准**；历史存疑的写法（如"重组前叫 XX"）**不要再写进文档**。

**⑥ 不要改写验收侧文档（协作边界）**
- `docs/06 §9–§11`（验收登记）与 `docs/07`（验收报告）**由验收侧维护**，编码侧**不得就地改写**；发现疑问 → 写进 `docs/08`，或在该处留「**待验收侧确认**」。
- 反过来，**`docs/09 §0–§7`、`docs/03`、`docs/05` 由编码侧维护**，验收侧只在**自己的小节**（如 `docs/09 §8/§10`）追加，不改对方正文。
- **`CLAUDE.md` 由编码侧维护**；验收侧需要加内容时**只做定点 patch**，不整份覆盖（2026-09-14 `59d4a0d` 整份覆盖曾吃掉本文的 ④③⑤⑥，已恢复）。
- `docs/08-待修清单.md` **双方共用**：各自只改自己负责的条目与己方附注，不改对方段落。

> **一条总规**：**改别人的文档，不如把证据留给对方**。事实分歧 → 附可复现的命令与输出；结论分歧 → 列进 `docs/08` 交用户拍板。
