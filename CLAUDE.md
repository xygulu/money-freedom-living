@AGENTS.md

## 开工前必读（2026-09-14 起）

**① 待修清单：`docs/08-待修清单.md`** —— **动手前先扫一遍**。这是**唯一的待修入口**。
含：P0 开跑前置 4 条 · P1 上线前置 7 条 · P2 接第二本书前 6 条 · P3 排期/体验 6 条 · **P4 文档与工程卫生（2026-09-14 已全部清掉，留痕与教训见 `docs/09 §10`）** ·
**附A 已闭环** · **附B ❌ 已撤销 = 不要照修** · 附C 优化建议 · **附D 怎么验（验收标准）**。

**①b 原始出处（要查证据时再看）**
- `docs/07-M11验收报告与优化建议.md` —— **验收侧**报告（`docs/07` 由验收侧生成，编码侧可能修改过）
- `docs/06 §9–§11` —— **验收侧独立核查**的原始登记与**撤销记录**（证据存档；§11 的「❌ 撤销」表列的是**不要照修**的项）
- `docs/09-技术方案一致性对照.md` —— `docs/03` ↔ 知识库 ↔ v1 现状；`docs/08` 里 P3 的详细依据

> **同一条问题在 `docs/07` 与 `docs/06` 都有时，以 `docs/08` 的合并条目为准**（已去重）。

> **两份验收文档冲突时怎么办**：`docs/07` 与 `docs/06 §9–§11` 都是验收结论。以**修订时间较新、且带可复现证据**的一方为准；`docs/06 §11`「❌ 撤销 / 复核后仍成立」是最新一次实测复核的结果，**列出的是不要照修的项目**。
> 冲突未解前，**先问用户或先把证据补上**，不要凭其中一份动手。

**② 方案文档（改行为前先对齐，不要凭印象）**
- `docs/05-多书架构与触达层.md` —— 多书架构 + 触达层（M11 主文档）。**枚举名等细节以代码为准**，该文 §0.1 是实现进度表。
- `docs/06-信息架构与认知层验收.md` —— 信息架构（一级入口 4→3、页面去向、"合"压成一屏）+ 认知层指标 L4/L5/M 的采集落点 + **§9–§11 = 独立验收结论（验收人登记）**：§10 待修清单 8 条、§11 公开环境实测。**其中 §11 的「❌ 撤销」表是本轮误报，不要照修。**
- `docs/03-技术方案.md` —— `§3` 表结构、`§10` 里程碑、`§11` 修订记录（**变更史**）。
- `docs/01` 定位 / `docs/02` 产品口径 / `docs/04` M8 Go-No-Go。

**③ 改了就要留痕**
- **每次改动在 `docs/03 §11 修订记录` 留一条**（产品口径同时同步 `docs/02 §12`）。
- 承重前提（人群 / 陪跑形态 / 领域焦点 / 书的可见度）被替换时，**通篇重写，不要打补丁**。
- 知识库（Obsidian）侧的同步由 Hermes 负责；**你只需保证 `docs/` 内部一致 + 留修订记录**。

**④ 三条纪律（都踩过，别再犯）**
1. **不得凭印象写引用**：文中要写"依据知识库 XX 文件 / 依据 docs/NN §X"之前，**必须核对它真实存在**。
   （2026-09-14 曾引用不存在的知识库文件 `20-6_信息架构_v4`；同日又凭印象写出一份"知识库真实文件清单"，把 `00-0`/`20-7`/`20-8`/`20-9`/`20-5`/`20-6` 说成不存在、把 `70_技术/70-1`/`80-1`/`80-2` 说成存在 —— **两端全反**。验收侧已在 `docs/09 §8` 逐条证伪。）
2. **不得代署他人的名**：验收结论只归验收人。
   写"Hermes 已验收 / 由 Hermes 登记"之前，**先确认真是对方写的**；自己发现的写成"待验收"。
   （2026-09-14 `docs/06` 初稿曾署"由 Hermes 补写"，实为编码侧起草。）
3. **核对要核到"写入真值的那一行"，不能停在"长得像的东西"**。
   `grep` 到一个同名类型就当真值 → 会既误删真取值、又误加假取值（2026-09-14 `safety_events.source` 被错判：真取值是 `chat｜journal｜letter｜experiment｜calibrate` 五个，`calibrate` **存在**，只漏 `experiment`；被引作依据的 `profile.ts:200` 其实是**另一张表**的 `ThreadEvidence.source`）。
   **验收侧同理**：`docs/06 §11` 的 3 条误报（坐标点击 + 长截图）也是这一族 —— **取证方式不支撑结论的强度。**

**⑤ 知识库（Obsidian）不属于本服务器，不要凭印象描述它**
- 知识库在**用户 Mac 上**（`~/Documents/老爸知识库/`）。**服务器上没有它的副本，也没有同步通道。** 本机只有一个 Obsidian 库（已实测）。
- 因此：**服务器侧文档不得断言知识库的文件名与内容**。要引用时，只引用知识库 `00-0_服务器文档与知识库同步对照表.md` 里**已登记过**的映射，或直接写「待验收侧确认」。
- **`80_验收/`、`80-1`、`80-2`、`00-1_文档同步台账`、`70_技术/`、`20-5_产品升级思路_v4_一条路.md`、`20-6_信息架构_v4_从四个房间到一条路.md` —— 这些名字在知识库里全部不存在，不要再引用**（2026-09-14 两次虚构建，见 `docs/09 §8.2 / §8.2b`）。真实存在的是 `20-5_产品架构_成长归用户与多书融合.md`、`20-6_v1数据模型改动清单.md`、`20-7_信息架构与认知层验收.md`、`20-8_M11系统验收报告与优化建议.md`、`20-9_待修清单.md`、`00-0_服务器文档与知识库同步对照表.md`。
- 知识库侧的同步由 Hermes（验收侧）负责；**你只需保证 `docs/` 内部一致 + 留修订记录**（原 ③ 末条，此处重申）。

**⑥ 不要改写验收侧文档**
- `docs/06 §9–§11`（验收登记）与 `docs/07`（验收报告）**的引用与结论由验收侧自己维护**，编码侧**不得就地改写**。
- 发现疑问 → 写进 `docs/08-待修清单.md`，或在该处留「**待验收侧确认**」，**不要替验收侧\"更正\"**。
- 踩过的坑：`893dca1` 把 `docs/07` 里**正确的知识库引用**改成了虚构路径，还加注"原来的名字不存在"——**下游会把它当成\"验收人自己更正过\"的结论读走**，比在服务器侧写错更危险。
